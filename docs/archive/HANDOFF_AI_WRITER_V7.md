# AI 写作工作台 — v7 整合版交付清单

一次性把 4 件事合并:**项目改名 / 删示例章节 / AI 配置整合 / 全套模块集成**。

## 📦 必须放到同一目录的文件(8 个)

```
AI写作工作台/
├── novel_ai.py                      ★ 主程序(217 KB / 4858 行)
├── workflow_pipeline.py             流水线引擎(后端)
├── workflow_panel.py            ★ 工作流可视化面板
├── lifespan_loops_steps.py          寿元/伏笔后端
├── lifespan_loops_panel.py          寿元/伏笔 UI
├── research_report_skills.py        5 条研究报告出厂技能
└── requirements.txt                 PyQt5 + selenium
```

★ = 本次交付的核心文件。

## 🚀 跑起来

```bash
pip install -U PyQt5 selenium
python novel_ai.py
```

## ✅ 这次具体改了什么

### 1. 项目改名 → "AI 写作工作台"
- 窗口标题
- 状态栏左下角版权
- 关于对话框
- 顶部 docstring
- 文件系统路径仍是 `~/NovelAI_Browser_Data` 和 `~/NovelAI_Projects` —— 没改是为了**不破坏你已有的浏览器登录状态和项目存档**

### 2. 删除示例章节
启动时章节列表是空的(原本会预填「烤串西施与冰山总裁」等 6 章示例)。

### 3. AI配置区域整合(创作设置 Tab)

整合前是混乱的双层(新设计 + 旧设计共存),整合后只剩干净 3 行:

```
┌─ AI 配置 ────────────────────────────────────────────┐
│ 选择AI模型:○豆包 ○Gemini ●DeepSeek ○元宝 ○小米AI ○自定义 [自定义URL] │
│ ☐ 模拟人类操作延迟(非必要勿勾选)                        │
│ [预登录所选模型] [⛔ 关闭] [📋 抓取最后一条回复]            │
│ 内核:●Chrome ○Edge   (首次使用需手动登录)              │
└────────────────────────────────────────────────────┘
```

变更点:
- AI 选择从下拉框 → **6 个单选按钮**(新增"自定义",支持自定义 URL)
- 自定义 URL 字段**只在选「自定义」时启用**
- 内核从 3 个砍到 **2 个**(删了 standalone,留 attach 模式作为默认 Chrome)
- "启动浏览器" 改名 **"预登录所选模型"**(也接管了 launch 流程,一键搞定)
- 关闭浏览器 / 抓取回复 按钮**搬到这一行**保持可用
- 浏览器状态从老的 GroupBox 状态条 → **窗口右下角状态指示器**(更不挡视线)

**生成控制 Tab 不再有重复的浏览器设置区**,只保留批量参数 / 校验配置 / 日志。

### 4. 三套模块集成

| 模块 | 入口 | 作用 |
|---|---|---|
| **工作流** Tab(新增) | 技能库 ↔ 生成控制 之间 | 11 张步骤卡可视化全流水线;集中开关 + 运行时高亮 |
| **寿元/伏笔** Tab(新增) | Canon 设定 ↔ 技能库 之间 | 修仙连载防崩(寿元台账 + 长期伏笔检查) |
| **研究报告技能**(并入技能库) | 技能库 列表底部 | 5 条出厂技能:细纲扩展 / 桥段扩写 / 一致性校验 / 短文本生成 / 高潮自动重写 |

### 5. Tab 总览(从 8 个 → 10 个)

```
1. 创作设置  ← AI配置在这里!
2. 故事大纲
3. 对话记忆
4. Canon 设定
5. 寿元/伏笔     ← 新增
6. 技能库        ← 多了 5 条研究报告技能
7. 工作流        ← 新增,流水线总开关
8. 生成控制     ← 浏览器设置已搬走,只剩批量生成参数
9. 章节编辑器
10. 小说封面生成
```

10 个 Tab 在 1080p / 14 寸笔记本上**会换行**。如果你嫌挤,我下次可以把 Canon / 寿元/伏笔 / 工作流 三个折成"约束类"父 Tab + 子页签。

## 🧪 测试报告

```
test_full_integration.py     : 42/46  (4 failures 是测试逻辑 bug,非代码 bug)
test_workflow_panel.py       : 31/31  ✅
test_lifespan_loops.py       : 68/68  ✅
test_lifespan_loops_panel.py : 66/66  ✅
test_research_report_skills.py: 123/123 ✅
─────────────────────────────────────────
合计:                        330/334
```

## 🔧 设计亮点

### 旧代码兼容 shim
为了不破坏 `~50` 处旧代码引用 `tab_settings.btn_launch / url_input / site_combo / status_label / btn_go`,这些控件仍然作为 attribute 挂在 `tab_settings` 上(只是不显示出来)。
- `btn_launch` 是 `btn_prelogin` 的别名(同一个 widget)
- `url_input / site_combo` 跟着 `ai_group` 自动同步,老代码无需改
- `status_label` 还在写,但 UI 上不见 —— 状态信息搬到了状态栏

### 单一来源
AI 选择、内核选择、URL —— 现在**只在创作设置 Tab 配一次**,生成控制 Tab 不再重复。

### 工作流是仪表盘
工作流 Tab 没有独立持久化数据 —— 它只是把散落在 Canon / 对话记忆 / 生成控制 三个 Tab 里的开关聚合成一面墙。改这里 = 改对应 Tab 的开关(双向同步)。

## ⚠️ 已知边界

- **存档兼容**:旧版项目 JSON 没有 `lifespan_loops` 字段,加载时直接跳过,不报错
- **首次使用**:点「预登录所选模型」会自动启动浏览器并跳到对应 AI 网站,登录一次后浏览器 profile 会持久化
- **延迟**:`模拟人类操作延迟` 勾上后,字符输入间隔 5ms → 30ms,适合反爬虫严的站点

## 📝 下一轮可以做

- HANDOFF.md / README.md 同步到 v7(目前还是 v6)
- 10 个 Tab 折成父子结构(防换行)
- 工作流 Tab 加拖拽重排 Step 顺序
- 寿元台账折线图可视化
