<!-- 项目对接记忆 v2.23.5 · 2026-05-27 更新 -->
<!-- 上一代记忆备份:项目对接记忆_backup_20260527.md -->

# NovelAI 盘古写作引擎 · 项目对接记忆

> **当前版本**: v2.23.5
> **最近更新**: 2026-05-27 · 番茄真实分类作为题材池 + 灵感采样升级 + 测试挪入 tests/
> **核心文件**: `novel_ai.py` (12185 行) + 19 个模块 + 66 个测试(在 tests/)
> **技术栈**: Python 3.12 + PyQt5 + Selenium + Chrome DevTools Protocol

---

## 一、项目身份

**NovelAI 盘古写作引擎** 是一个 AI 辅助网文写作桌面应用。

核心能力：
- **自动章节生成**：通过浏览器操控 AI 站点(DeepSeek/Qwen 等)批量生成网文章节
- **双 AI 分工**：主 AI(DeepSeek)负责章节生成，副 AI(Qwen)负责摘要/Canon 抽取等轻任务
- **质量守护**：写后自动检查(钩子检测/字数/信息泄露/承诺追踪等)，不合格自动重写
- **设定一致性**：Canon 设定库 + 角色库 + 伏笔追踪，防止跑设
- **番茄榜单分析**：扫描番茄小说 74 个分类榜单，用真实数据辅助创意灵感生成

**不是什么**：这不是一个 API 调用的 AI 应用——它通过 Selenium 操控真实浏览器，在 AI 网站的 Web UI 上打字、发送、抓取回复。所有 AI 交互都走浏览器 DOM 操作。

---

## 二、技术架构

```
┌─────────────────────────────────────────────────────┐
│                    MainWindow (PyQt5)                │
│  ┌─────────┬──────────┬──────────┬────────────────┐  │
│  │创作设置 │世界构建  │角色系统  │生成引擎        │  │
│  │         │·故事大纲 │·角色与世界│·生成控制       │  │
│  │         │·对话记忆 │·技能库   │·工作流         │  │
│  │         │·Canon设定│          │                │  │
│  ├─────────┼──────────┼──────────┼────────────────┤  │
│  │伏笔检查 │章节编辑器│拆书学习  │📊番茄榜单     │  │
│  └─────────┴──────────┴──────────┴────────────────┘  │
│                         ↓ 信号                        │
│  ┌─────────────────────────────────────────────────┐  │
│  │              BrowserWorker (QThread)             │  │
│  │  Selenium → Chrome DevTools → AI 网站 DOM 操控  │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 关键线程模型

- **主线程 (UI)**: PyQt5 事件循环，所有 UI 操作
- **Worker 线程**: `BrowserWorker(QThread)`，独占浏览器操控
- **通信**: PyQt5 Signal/Slot，主线程 → worker 用 `task_queue`，worker → 主线程用 `pyqtSignal`

### AI 交互流程

```
用户点"生成" → 主线程拼 prompt → worker.submit(task)
→ worker: _goto(AI站点) → 注入 textarea → 按 Enter
→ polling 循环: 每秒抓一次回复文本，判断"内容稳定" → 完成
→ emit response_received → 主线程: 质量检查 → 通过则入库
```

---

## 三、功能清单

### 3.1 章节生成与管理

| 功能 | 说明 | 关键代码 |
|------|------|----------|
| 单章生成 | 拼 prompt → AI 生成 → 质量检查 → 入库 | `_send_to_ai`, `_on_response_received` |
| 批量生成 | 连续生成多章，自动衔接上下文 | 生成控制 Tab |
| 章节编辑器 | 手动编辑/预览/导出 | `ChapterEditor` |
| 章节锁定 | 锁定已定稿章节防误改 | BUG-066, `locked` 字段 |
| 自动保存 | 定时 + 操作触发保存到项目文件夹 | `_autosave` |

### 3.2 质量守护(盘古系统)

| 功能 | 说明 | Prompt 键 |
|------|------|-----------|
| 写后质量检查 | 检查钩子/字数/信息泄露等 | `_check_chapter_quality` |
| 章节摘要 | 每章生成摘要供后续章节参考 | `chapter_summary` |
| 十三法点评 | AI 从 13 个维度评分 | `critique_rhythm` |
| 老刀点评 | 犀利文风点评 + 自动修复 | `critique_laodao`, `laodao_autofix` |
| Canon 审查 | 检查设定一致性 | `canon_audit`, `canon_extract` |
| 信息泄露检查 | 防止剧透/提前揭示 | `info_check`, `info_disclose_check` |
| 承诺追踪 | 检查"许诺了但没兑现"的情节 | `promise_check`, `promise_reeval` |
| 伏笔检查 | 跟踪伏笔埋设/回收 | `foreshadow_check`, `foreshadow_reeval` |
| 全书巡检 | 一键扫描所有章节的健康度 | 工具栏"🛡️ 全书巡检" |
| 管家日报 | 本次会话章节健康汇总 | `housekeeper.py` |

### 3.3 世界构建

| 功能 | 说明 |
|------|------|
| 故事大纲 | 种子/世界观/结构/分章大纲/开篇 |
| 对话记忆 | 角色记忆/摘要记忆/长期记忆(自动注入 prompt) |
| Canon 设定 | 设定守护库，AI 每次生成时参考 |
| 角色与世界 | 角色库(姓名/性格/外貌/关系) + 关系图谱 |
| 寿元台账 | 修仙类特有：寿元/境界/功法追踪 |
| 伏笔管理 | 伏笔埋设章/回收章/超期预警 |

### 3.4 创意与灵感

| 功能 | 说明 |
|------|------|
| 创意灵感生成 | AI 出 5 个创意方向供选择 |
| **番茄榜单扫描** | 74 个分类榜单 × Top10 = 740 本，真实数据驱动灵感 |
| 风格库 | 风格匹配/推荐(当前只显示不注入 prompt,待做) |
| 差异化分析 | 帮用户找差异化卖点 |

### 3.5 番茄榜单系统(v2.23.x)

```
程序启动 30s → 后台自动扫 74 榜(可关闭)
                    ↓
           rank_snapshot 写磁盘(24h 缓存)
                    ↓
           后台抓 Top5 详情页(7 天缓存)
                    ↓
         📊 番茄榜单 Tab 实时展示:
         · 男频/女频题材热度排行(金银铜高亮)
         · 书单详情表(题材筛选下拉)
         · 进度条 + 3 个按钮(刷新/补抓/打开缓存)
                    ↓
         用户点"生成灵感" → 榜单数据拼入 prompt
                    ↓
         v2.23.5 升级:
         · 题材选择 UI 用番茄真实分类(37 个,男/女/通用三组)
         · 智能题材匹配(玄幻 → 传统玄幻+玄幻脑洞+玄幻言情+都市高武)
         · 灵感采样 8 → 20 本,简介 80 → 300 字
         · 样本按分类分组展示(便于 AI 看每个题材的钩子手法)
```

### 3.6 AI 站点集成

| 站点 | 用途 | Profile 键 |
|------|------|------------|
| DeepSeek | 主 AI(章节生成) | `chat.deepseek.com` |
| Qwen 通义千问 | 副 AI(摘要/Canon/灵感) | `chat.qwen.ai` |
| ChatGPT | 备选 | `chatgpt.com` |
| 豆包 | 备选 | `doubao.com` |
| Gemini | 备选 | `gemini.google.com` |
| 腾讯元宝 | 备选 | `yuanbao.tencent.com` |

每个站点在 `core/site_profiles.py` 有独立 profile：发送按钮 selector、回复容器 selector、停止按钮 selector 等。

### 3.7 工具集

| 工具 | 入口 |
|------|------|
| A/B 对比重写 | 工具栏 |
| AI 智能取名(全文替换) | 工具栏 |
| 情绪曲线 | 工具栏 |
| 剧情线地图 | 工具栏 |
| 模拟读者评审 | 工具栏 |
| 转短剧剧本 | 工具栏 |
| 拆书学习 | 独立 Tab |
| DOM 诊断 | 工具栏(调试用) |
| RL 学习状态查看/重置 | 工具栏 |

### 3.8 RL 自学习(flow_rl.py)

强化学习模块，基于 (任务类型 × 站点 × 重试次数) 状态空间学习最优参数：
- `send_wait`: Enter 后等多久确认发送
- `stable_wait`: 内容稳定多久判完成
- `idle_multiplier`: 空闲超时倍率
- `use_strategy_b`: 是否用策略 B 发送

每次任务完成后自动更新 Q 值，持久化到 QSettings。

---

## 四、文件结构

```
novel_ai/
├── novel_ai.py              # 主程序(12185 行) — MainWindow + 所有 UI 逻辑
├── core/
│   ├── constants.py          # 全局常量
│   ├── default_skills.py     # 出厂技能模板
│   ├── fanqie_genre_provider.py # (v2.23.5)题材池单一数据源 + 智能匹配
│   ├── fanqie_rank_scraper.py # 番茄榜单扫描 + 解析 + 缓存
│   ├── prompts.py            # 31 个 Prompt 模板
│   ├── site_profiles.py      # 8 个 AI 站点的 DOM 选择器配置
│   └── stylesheet.py         # Qt 样式表
├── ui/
│   ├── browser_worker.py     # 浏览器 Worker 线程(Selenium 操控)
│   ├── fanqie_rank_tab.py    # 📊 番茄榜单 Tab
│   ├── foreshadow_tab.py     # 🪤 伏笔检查 Tab
│   ├── ab_compare.py         # A/B 对比重写
│   ├── ai_naming.py          # AI 智能取名
│   ├── conversation_switcher.py # 对话切换器
│   ├── debug_panel.py        # 调试面板
│   ├── emotion_curve.py      # 情绪曲线
│   ├── plot_timeline.py      # 剧情线地图
│   ├── project_launcher.py   # 项目启动器
│   ├── reader_panel.py       # 阅读面板
│   ├── story_outline.py      # 故事大纲
│   ├── highlighters.py       # 语法高亮
│   └── theme.py              # 主题切换
├── pangu_system.py           # 盘古引擎(prompt 增强 + 元信息解析)
├── pangu_patch.py            # 盘古引擎补丁
├── housekeeper.py            # 管家系统(章节健康巡检)
├── dialogue_critic.py        # 对话质量评审
├── flow_rl.py                # RL 自学习(参数调优)
├── workflow_panel.py         # 工作流面板
├── workflow_pipeline.py      # 工作流管线(步骤链)
├── relation_graph.py         # 角色关系图谱
├── book_splitter.py          # 拆书学习
├── import_continuation.py    # 导入外部小说续写
├── lifespan_loops_panel.py   # 寿元台账面板
├── lifespan_loops_steps.py   # 寿元台账逻辑
├── research_report_skills.py # 研究报告技能
├── project_io.py             # 项目文件夹读写
├── tts_backend.py            # TTS 语音合成后端
├── license_guard.py          # 授权守卫
└── tests/                   # 测试目录(66 个测试文件)
```

---

## 五、浏览器 Worker 核心机制

### 5.1 连接模式

Chrome Attach 模式：先起一个带 `--remote-debugging-port=9222` 的 Chrome 实例，然后 Selenium 通过 DevTools Protocol attach 上去。用户数据目录独立(`NovelAI_Browser_Data`)，不影响日常浏览器。

### 5.2 Polling 抓取

```python
while True:
    cur = grab_last_response()  # 抓最新回复文本
    if cur == last_text:
        stable_seconds += interval
    else:
        stable_seconds = 0
        last_text = cur
    
    # 完成判定(三层):
    # C. Qwen 思考指示器(确定性信号)
    # A. 站点 stable_wait_min(时间下限)
    # B. 站点 min_complete_chars(字符下限)
    if stable_seconds >= threshold and len(cur) >= min_chars:
        break
```

### 5.3 BUG 修复链(v2.22.x ~ v2.23.x)

| BUG | 版本 | 问题 | 修法 |
|-----|------|------|------|
| 081 | v2.22.0 | Qwen 发送按钮通用 selector 不命中 | 站点 profile `send_btn` 优先 |
| 082 | v2.22.1 | Qwen 流式输出 0.9s 就判完成(只抓到 17 字) | thinking_indicator + stable_wait_min + min_complete_chars 三层守卫 |
| 083 | v2.22.2 | 卡死提醒误报(AI 还在写就弹窗) | task_progress 信号 + 0 字节判定 |
| 084 | v2.22.3 | BUG-082 守卫触发后死循环(3.5 分钟 200+ 圈) | 守卫累计超时 90s + sleep 3s + 日志抑制 |
| 085 | v2.22.3 | Qwen 思考期 _check_timeout 误报 | task_thinking 信号 + 思考期延期 60s |
| 086 | v2.23.0 | 钩子检测死磕 10 次(元信息声明了但关键词没命中) | 元信息 ★★★★+ 直接放行 |
| 087 | v2.23.2 | _goto 同 host 跳过 → 扫榜 74 次从没 navigate | 改用 driver.get() 强制 navigate |
| B | v2.23.4 | 扫榜/详情抓取覆盖 DeepSeek 标签 | 独立新标签执行,完了关掉切回 |
| C | v2.23.4 | _ensure_ai_tab_alive 遍历后 driver 停错位置 | 保存/恢复 handle |
| ─ | **v2.23.5** | 题材选项跟番茄真实分类脱节(用户选"玄幻"匹配不到"传统玄幻"等) | **新增 `core/fanqie_genre_provider.py`:37 个番茄真实分类按男/女/通用分组 + 智能别名匹配** |
| ─ | **v2.23.5** | 灵感采样太薄(8 本 × 80 字简介,AI 没真实标尺) | **max_samples 8 → 20, 简介 80 → 300 字, 显示书名(配"绝不复制"约束), 按分类分组** |
| ─ | **v2.23.5** | 启动器是营销落地页风格(6 个无用功能卡片 + Hero 浪费空间 + 显示 autosave/migrated 等垃圾) | **重写为"用户启动页":去掉 FeatureCard,改用项目卡片网格(章节数+时间徽章),空状态友好引导,过滤系统目录** |

---

## 六、关键设计决策(不要打破)

1. **浏览器只有一个,Worker 独占** — 所有浏览器操作必须走 `worker.submit(task)`，主线程不能直接 `driver.xxx()`

2. **扫榜/详情在独立标签** — `window.open('about:blank')` 开新标签，完事 `driver.close()` + 切回。**绝不在 AI 标签上做非 AI 操作**

3. **_goto 的同 host 跳过优化** — 双 AI 切换时复用标签(不开新的)。但扫榜/详情不能用 `_goto`，必须 `driver.get()` 强制 navigate

4. **Qwen 三层完成判定** — `thinking_indicator` (主) → `stable_wait_min` (兜底) → `min_complete_chars` (兜底)。缺任何一层都会误判

5. **_fanqie_scanning 锁** — 防止自动扫 + 手动扫同时跑。任何提交扫榜任务前检查,完成后清除

6. **项目文件夹格式** — `project_io.py` 管理，`<项目名>/project.json` + 章节文件。不是单 JSON

7. **31 个 Prompt 模板** — 在 `core/prompts.py`，`pangu_system.wrap()` 增强。改 prompt 格式要同时改 parser

8. **测试守护** — 每个 BUG 修复都带守护测试(test_bugXXX_*.py)。改代码前先跑 `pytest tests/test_bug*.py`

9. **题材是单一数据源**(v2.23.5) — `core/fanqie_genre_provider.py` 是题材池的唯一来源。37 个番茄真实分类按男/女/通用三组,UI 和灵感采样都用同一份数据。改一处全联动。`match_user_genres_to_fanqie()` 是用户题材 → 番茄分类的智能映射,**所有匹配逻辑走它**,不要在别处再写一遍。

---

## 七、commit 历史

| commit | 版本 | 内容 |
|--------|------|------|
| `(待推)` | **v2.23.5** | **番茄真实分类作为题材池 + 智能匹配 + 灵感采样升级** |
| `8ad3f88` | v2.23.4 | BUG 扫描: 扫榜/详情独立标签 + handle 恢复 + signal 类型 |
| `ead70ac` | v2.23.4 | 详情独立标签 + DeepSeek 自动恢复 + 多显示器分辨率修复 |
| `80098e8` | v2.23.4 | 自动扫榜开关(QSettings 持久化) |
| `a7ffbe3` | v2.23.4 | 补抓失败详情按钮 + 扫榜防重复锁 |
| `13d381e` | v2.23.4 | _fanqie_scanning 防重复 + 手动刷新清缓存 |
| `cd61a26` | v2.23.4 | 在读数据修复(从 rank_snapshot 读) + 题材筛选下拉 |
| `c027c73` | v2.23.4 | 📊 番茄榜单 Tab(热度表 + 详情表 + 按钮) |
| `826aaeb` | v2.23.3 | 详情深度抓取 + 后台 scheduler + 磁盘缓存 + UI 修复 |
| (更早版本在 shallow clone 之前,需 `git fetch --unshallow` 查看) |

---

## 八、已知问题 / TODO

### 待修复

| 项 | 优先级 | 说明 |
|----|--------|------|
| 风格库不注入 prompt | 🟡 中 | `pangu_system.build_style_report` 只显示不注入。需走 B 方案(用户选风格下拉) |
| v2.12-v2.21.5 历史债务 | 🟢 低 | 12+ 个版本的记忆没补(双 AI 分工/关系图/伏笔/智能冷却/隐藏浏览器/自动关机/4K 适配等) |

### 待做(新功能)

| 项 | 说明 |
|----|------|
| 详情页深度抓取增强 | 番茄详情页有干净简介/标签,但 parse_detail_page_html 在实战中可能没解析到(React 渲染差异) |
| 多平台扫榜 | 七猫/起点/晋江,沿用 scraper 架构 |
| 历史榜单趋势分析 | 跨天对比榜单变化 |
| 批量锁定/解锁 | 目前只能单章右键,可加"锁定前 N 章" |

---

## 九、用户偏好(从合作中观察到的模式)

1. **"按你推荐的来"** — 用户信任 Claude 的技术判断,但要求先说清楚方案再动手
2. **一次只做一件事** — 不要在一个 commit 里混多个不相关改动
3. **PAT 推送** — 用户习惯给 PAT 让 Claude 直接推,但每次推完要提醒撤 PAT
4. **实战验证第一** — 沙箱测试通过不算数,用户电脑上跑通才算
5. **记忆必须同步** — 每次推新版本都要更新项目对接记忆
6. **不要过度解释** — 用户是技术人员,简洁直接

---

## 十、接班便条(给下一个 Claude)

### 你拿到这份记忆时要做的第一件事

1. `git log --oneline -20` — 看最新 HEAD 是不是 `8ad3f88`(v2.23.4)
2. 问用户"上次跑得怎么样,有没有新日志" — 实战日志是唯一可信的 bug 来源
3. 不要假设任何东西 — v2.23.1 的 cat_id 就是我"假设"出来的,全错

### 踩过的坑(别再踩)

1. **沙箱 push 不稳** — 看起来成功但远端没变。好消息是最近 4-5 次都真成了
2. **番茄字体反爬** — 榜单页简介/书名是乱码(obfuscated font),只有 book_id + 在读数是干净的。详情页 `/page/{id}` 的 meta 标签是干净的
3. **`_goto` 同 host 跳过** — 扫榜/详情必须用 `driver.get()`,不能用 `_goto`
4. **Qwen 思考阶段** — 80 秒 0 字符是正常的(在思考),不是卡死
5. **git pull 不等于代码生效** — Python 进程必须重启,光 pull 不够
6. **测试硬编码版本号** — 升版本后旧测试可能断言 `APP_VERSION == "v2.23.3"`,需要松绑

### 当前代码健康度

- 66 个测试文件，统一在 `tests/` 目录，最近一次回归: **274 passed, 2 pre-existing failed**(PyQt5 导入问题)
- 0 个未覆盖的 known bug
- 风格库注入是唯一的"功能空洞"(显示了但没用)

---

*最后更新: 2026-05-27 by Claude (v2.23.5 `81200e3` · 测试统一移入 tests/)*

---

## 十一、工作流程手册（整合自 mattpocock/skills）

> 以下流程卡来自 https://github.com/mattpocock/skills，根据本项目实际情况适配。
> 每张卡是给 Claude 的操作规程，遇到对应场景直接按步骤走。

---

### 🔍 DEBUG 流程（diagnose skill）

遇到 bug 或报错，**按六个阶段走，不能跳过**：

**Phase 1 — 建立反馈回路（最重要）**
先搞定一个能稳定复现 bug 的最小脚本/命令，才能进下一步。
优先顺序：
1. 跑已有测试 `QT_QPA_PLATFORM=offscreen python -m pytest tests/ -k "关键词" -x`
2. 写一个最小复现脚本 `python scripts/repro_xxx.py`
3. 检查 `scripts/pre_push_check.py` 能不能拦住

没有可复现的回路 → 停下来，告诉用户需要什么信息，不要乱猜。

**Phase 2 — 复现**：运行回路，确认报错和用户描述一致。

**Phase 3 — 提假设**：列出 3-5 个有排序的假设，每条写成"如果是X导致的，那么Y会发生"。**先给用户看，再去验证**。

**Phase 4 — 定点探针**：每次只改一个变量。日志加唯一前缀 `[DEBUG-xxxx]`，修完统一删。

**Phase 5 — 修复 + 回归测试**：先写失败的测试，再改代码，让测试变绿。

**Phase 6 — 清理**：删所有 `[DEBUG-...]` 日志，确认原始 repro 不再复现，commit message 里写清楚是哪个假设被证实了。

---

### 📋 新功能规划流程（grill-with-docs skill）

做任何超过 50 行的改动前，先过这个清单：

1. **对齐术语** — 新功能涉及的概念，和项目对接记忆里的术语一致吗？有没有用词模糊的地方？
2. **质问方案** — 这个方案有没有更简单的替代？改动面有多大？影响哪些模块？
3. **确认边界** — 改这个模块会不会连带影响其他地方？（尤其是 `novel_ai.py` 和 `browser_worker.py`）
4. **不变量检查** — 六大设计决策（见第六章）有没有被破坏？
5. **测试先行** — 先写守护测试的骨架，再动代码

**本项目专属项目术语表**（避免歧义）：

| 术语 | 含义 |
|------|------|
| Worker | `BrowserWorker(QThread)`，独占浏览器，所有 Selenium 操作在这里 |
| 主线程 | PyQt5 事件循环，所有 UI 操作，不能直接碰 driver |
| task_queue | 主线程向 Worker 提交任务的队列 |
| Profile | `SITE_PROFILES` 里的单个站点配置（selector 集合） |
| Canon | 设定守护库，AI 每次生成时参考 |
| 守护测试 | `tests/test_bugXXX_*.py`，每个 bug 修复必带 |
| 扫榜 | 抓番茄 74 个分类榜单，用 `driver.get()` 不能用 `_goto` |
| 详情抓取 | 抓每本书的 `/page/{book_id}` 页面，需要按间隔限速 |
| DEV_MODE | 跳过授权验证，测试阶段默认开启，上线前关闭 |
| 原子写入 | `_atomic_write()`，先写临时文件再 `os.replace`，防止写坏 |

---

### 🔄 会话交接格式（handoff skill）

每次结束一轮工作，在 commit message 或对话末尾写：

```
## 本次完成
- [commit hash] 具体做了什么

## 下次接班要做的第一件事
- git log --oneline -5 确认最新 HEAD
- 问用户"上次跑得怎么样，有没有新报错"

## 当前悬而未决的问题
- [如果有]

## 不要碰的地方
- [如果有]
```

---

### 🧪 TDD 流程（tdd skill）

修 bug 或加功能时，**优先红绿重构**：

1. **红**：先写一个失败的测试，放在 `tests/test_bugXXX_描述.py`
2. **绿**：最小改动让测试通过
3. **重构**：在测试保护下清理代码
4. **跑全套**：`QT_QPA_PLATFORM=offscreen python scripts/pre_push_check.py`

守护测试命名规范：
- Bug 修复：`test_bugXXX_简短描述.py`
- 版本功能：`test_vX_Y_Z_功能描述.py`
- 一致性检查：`test_site_url_consistency.py` 这类

---

*十一章更新: 2026-05-27 · 整合 mattpocock/skills 工作流*
