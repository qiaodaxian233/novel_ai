<!--
项目对接记忆.md — 给下一代 Claude 的接班手册
v2.07.2 (Task 2) 起整理:从 7174 行瘦到 ~1100 行。
老 BUG 修复历史(BUG-001~064 / v1.74~v1.89)已归档到:
  docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md

主记忆保留:
  - 头部(用户 / 项目目标 / 项目当前状态 / 关键设计决策 / 用户偏好)
  - 给下个 Claude 的具体警告(铁律级)
  - v1.90 P1 管家(P2/P3 扩展点跟新功能直接相关)
  - v1.91~v1.95 最近活跃 BUG 历史(BUG-065~069)
  - v2.x 模块化拆分历史(v2.00~v2.07.1 已在头部"项目当前状态")
  - 末尾更新协议
-->

# novel_ai 项目对接记忆 · 盘古超级系统

> **给下一个 Claude(或未来的我)**:这个文件是 `qiaodaxian233/novel_ai` 项目的完整快照。
> 用户换对话时让你读这个就能续接,不用从头看 7000 多行代码。

---

## 📋 长会话接班便条(2026-05-22 压缩快照)

> **这一段是上一次长会话末尾的压缩总结,新会话开头看完就基本掌握全貌。**
> 详细内容仍在下面各章节里,这段只是导航 + 关键提炼。

### 项目身份

- **GitHub**:`qiaodaxian233/novel_ai`(public,master 分支)
- **当前版本**:**v2.23.0**(待推,基于 `776f7c0` v2.22.3)
- **主程序**:`novel_ai.py` 9782 行(从 v1.x 的 20740 行瘦了 53.5%)
- **总测试**:1223+ 个,49+1 个测试文件,零退化纪律严格

### 2026-05-22 整轮推送链(早 → 晚)

| commit | 版本 | 内容 |
|---|---|---|
| `fd1b0eb` → `818d814` | v2.00→v2.05 | P1~P6 模块化拆分(主程序 20740→9653) |
| `c5bb8c6` | v2.06 | BUG-073 + BUG-074 双 bug 修复(P3 漏 PyQt5 imports + QSettings None 兜底) |
| `65967d9` | v2.07 | 全系统集成测试 + 16 测试静态扫描适配 + README 重写 |
| `6db97d8` | v2.07.1 | Task 1: 5 份过期 docs 归档到 `docs/archive/` |
| `858ed1a` | v2.07.2 | Task 2: 项目对接记忆 7174→1137 行(瘦 84%) |
| `1dc25e7` | v2.08 | Task 3-P2: Housekeeper 三扩展点(**另一会话推**) |
| `395ab83` | v2.09 | Task 3-P3: Housekeeper 两扩展点 |
| `184c81e` | v2.10 | 主程序集成 4 个 P2/P3 hooks + `DEFENSE_FINGERPRINTS` 字典 |
| **`368c0bd`** | **v2.11** | **实战 BUG-076:`_profile_for_url` import 漏(P6 拆分留)** |
| (中间 v2.12 → v2.21.5 12+ 个版本未补到记忆,见下文"历史债务"段) |  |  |
| **`f37accf`** | **v2.22.0** | **实战 BUG-081:Qwen 副 AI 按钮检测漏(JS 没用上站点 send_btn selector)** |
| `1aaf423` | **v2.22.1** | **实战 BUG-082:Qwen 流式输出慢被 polling 误判完成(17 字 JSON 半句)+ v2.22.0 收尾(版号 / 记忆)** |
| `5963555` | **v2.22.2** | **实战 BUG-083:卡死提醒误报 — 90 秒纯时间触发改为 0 字节 90 秒才触发** |
| `776f7c0` | **v2.22.3** | **实战 BUG-084/085 双修:① BUG-082 守卫死循环(3 分 35 秒 200+ 圈) ② Qwen 思考期 _check_timeout 误报"卡住"** |
| (待推) | **v2.23.0** | **BUG-086 新功能 + 修复:① 番茄榜单扫描增强灵感生成(独立 scraper 模块 + worker action + 30 分钟缓存 + 失败兜底)② 钩子检测松绑(元信息 ★★★★+ 放行 — 修第 1 章死磕 10 次)** |

### 关键架构

```
core/         9 文件 — 常量/QSS/PROMPTS/SITE_PROFILES/DEFAULT_SKILLS
ui/           7 文件 — 5 小类 + browser_worker + tabs/
ui/tabs/      9 文件 — 8 个 Tab + character_library
novel_ai.py   9782 行 — MainWindow 装配中心 + main()
housekeeper.py 743 行 — P1~P3 全部扩展点已实现
```

### 重要踩坑教训(已经吃过的亏,别再吃)

- **BUG-074 / BUG-076 同根因** — P3~P6 拆分时 AST 抽取脚本"外部依赖识别"覆盖不全:
  - BUG-074 漏 PyQt5 类 import(`Qt`/`QFont` 等)
  - BUG-076 漏 module-level 函数 import(`_profile_for_url`)
  - **下次拆分必检**:每个抽出的 .py 顶部 import 是否覆盖了它实际用的所有外部名字
- **静态测试抓不到运行时 NameError** — `test_full_integration` 实例化 MainWindow 但不真发 prompt,所以拆分后没在静态环境暴露;实战 14:37:40 一秒钟就挂
- **不要做雄心勃勃的"通用 NameError 巡检"** — 我试过了,误报 1100+(comprehension target / 多层作用域 / 动态属性全要处理),改成"已知问题精准守护"才对(BUG-076 用 5 行精准测试就守住了)
- **`DEFENSE_FINGERPRINTS` 是知识沉淀** — `novel_ai.py` 顶部字典(8 BUG × 13 patterns),`hk.verify_defenses()` 每 10 章自检,**修完棘手 BUG 就加进去**
- **沙箱重置 = 工作全丢** — 任何 commit 不立刻 push 就再见(记忆里 v1.97 教训)
- **PAT 不复用** — 用户每次给独立 fine-grained PAT,push 完撤;**不要把 PAT 写到任何文件**,也不要在日志里 echo,推送时嵌在 URL 里
- **GitHub Web UI 文件列表截断** — Web 看仓库会漏文件(只显示约 20 个),必须 clone 才知道真实文件数(本项目实际 76+ 文件)

### v1.90 P1 管家扩展点完成度

- ✅ #1-#5,#10,#11(P1 完成)
- ✅ #6 locked 巡检 / #7 节奏雷达 / #9 备份快照(P2 完成,v2.08)
- ✅ #10 RL 反馈 / #12 二道闸(P3 完成,v2.09 + 主程序集成 v2.10)
- 🟡 `snapshot_for_recovery` 实现了但未集成主程序(本项目用 single json 文件存档,跟 `project_root` 目录概念不匹配。要做需要先重构存档结构,或换 BUG-075 风格的 gzip+JSON)

### 用户偏好(高频出现的模式)

- 喜欢"按你推荐"的直接决策,**不喜欢冗长解释**
- "全做"+"一个一个来"的节奏(给一组方向,按推荐顺序一项项推进)
- 每次给独立 fine-grained PAT,push 完撤(铁律)
- 长期记忆 → `项目对接记忆.md`,**老 BUG 历史** → `docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md`(6102 行,按需读)
- 过期 docs → `docs/archive/`(5 份 v7 时代,按需读)

### 给下一次会话的开场白(用户可直接复制贴)

```
继续 novel_ai 项目(qiaodaxian233/novel_ai,master)。
当前 v2.22.3(BUG-084/085 已修),HEAD = 776f7c0。
git clone 后先看 项目对接记忆.md 头部的"长会话接班便条"段,
然后看"项目当前状态"段的最新里程碑详情。
```

---

## 👤 用户信息

- **GitHub**: qiaodaxian233
- **仓库**: https://github.com/qiaodaxian233/novel_ai(public,默认分支 `master`)
- **本地路径**: Windows(具体路径未询问)
- **沟通语言**: 中文(默认);技术回复直接、不啰嗦
- **协作模式**: 用 PAT 临时 push 流程(与同用户 `ai-music-lab` 项目一致)
  1. 用户去 https://github.com/settings/tokens?type=beta 建 fine-grained PAT
     (`Repository access` 只勾该仓库 / `Contents: Read and write` / 短期过期)
  2. 用户把 PAT 粘给 Claude
  3. Claude 在 `/tmp/push_work` clone + commit + push,PAT 嵌在 URL,不写文件不 echo
  4. 推完立刻 `rm -rf /tmp/push_work` + `unset GIT_PAT` + grep 残留扫描
  5. 用户立刻去 settings/tokens 撤销 PAT
  6. Claude 跑 `git push --dry-run` 验证 PAT 已死(`Authentication failed`)
- ⚠️ **铁律:每次 push 必须同步带上 `项目对接记忆.md` 的更新**
  - 用户的 Claude 对话额度有限,经常被"对话额度不足"卡断
  - 卡断后新会话开始 → 下个 Claude **完全靠这个文件续接**
  - 因此每个 push 都必须包含两类改动:
    - (1) 实际代码改动
    - (2) `项目对接记忆.md` 同步更新(里程碑 / BUG 历史 / 警告 / 未做事项)
  - **不要**先推代码再说"下次推记忆" —— "下次"可能就是新 Claude,记忆缺口就在那里
  - 唯一例外:**纯文档** push(像本条规则更新这种)可以单独推
- **用户偏好**:文件交付走 GitHub commit,**不是** zip / present_files
  (用户在某些客户端下载链接不可用 → 直接 push 比让用户 copy-paste 体验好)

---

## 🎯 项目目标

**本地 AI 写作工作台**(PyQt5 + Selenium):
- 挂载真实 Chrome/Edge,自动操作 DeepSeek / 豆包 / Gemini / 元宝 / ChatGPT 等 AI 网页
- **两种**浏览器内核:`Chrome 调试 attach`(推荐,程序与浏览器解耦) / `系统 Edge standalone`
- 章节列表 / 项目存档(JSON)/ 一键保存全章节
- 多题材 / 多平台 / 黄金三章 / 字数死磕 / 模拟人类延迟
- **盘古超级系统集成**(29 套子系统融合,2026-05-15 接入,同日加 GUI 面板)

---

## 📂 项目当前状态

**最新里程碑**:`(待推)` — **v2.23.0 🚀 新功能 + bug 修复:① 番茄榜单扫描增强灵感生成 ② 钩子检测松绑(修第 1 章死磕 10 次)**:

### BUG-086 第一组:番茄榜单扫描增强灵感生成

- **背景**:旧 `gen_inspiration` prompt 只是嘴上说"参考{platform}当前{genre}类热门榜",
  AI 是凭训练数据估算的"应该爆"的套路,跟当下真实榜单可能完全脱节。
- **目标**:点"生成灵感"时先扫 https://fanqienovel.com/rank?enter_from=menu 抓真实
  榜单,按用户题材过滤,拼增强 prompt 喂给 AI。
- **架构**(信号驱动 + 模块化):
  1. **`core/fanqie_rank_scraper.py`(新增 460+ 行)**:纯逻辑模块,**不依赖 Qt/Selenium**,
     可独立单测。导出 `Book`/`FanqieRankCache`/`filter_by_genres`/
     `build_enhanced_inspiration_prompt`/`parse_scraped_books`。
  2. **`ui/browser_worker.py`**:加 `rank_scraped = pyqtSignal(str, list)` 信号 +
     `scrape_fanqie_rank` action + `_scrape_fanqie_rank` 方法(navigate + 滚动让懒加载
     出现 + JS 多重 selector 抓取 + 失败 emit 空 list)。
  3. **`novel_ai.py`**:`gen_inspiration` 改成分支(平台是番茄+浏览器就绪→扫榜,
     否则旧通用 prompt),加 `_on_fanqie_rank_scraped` slot + `_gen_inspiration_send_fallback`
     + `_gen_inspiration_try_scrape` + `_gen_inspiration_with_books` 四个辅助方法。
- **取舍敲定**(都已实现):
  1. **扫榜失败兜底**:任何异常 → emit 空 list → 主进程 fallback 旧通用 prompt,不打断用户
  2. **30 分钟缓存**:`FanqieRankCache(ttl_sec=1800)`,同 platform + sorted(genres) 命中复用
  3. **抓 Top50 + 题材匹配**:hard match 不够 10 条 → 放宽到 `GENRE_RELAX_MAP` 相近题材
  4. **不传具体书名给 AI**:`build_enhanced_inspiration_prompt` 只传 Top10 题材组合 + Top15 卖点
     关键词,prompt 里加铁律 "不要直接复制任何具体作品或情节"
- **DOM 抓取策略**:多重 selector 候选(.rank-list .rank-item / .bookshelf-item / 模糊匹配)
  + 启发式兜底(找 `a[href*="/page/"]` 然后向上找父容器)。番茄改 DOM 时只需调 selector 一次。
- **数据流**:
  ```
  点"生成灵感"(番茄+ready)
    ↓
  FanqieRankCache.get() 缓存命中 → 直接拼 prompt 发 AI
                       未命中 → worker.submit(scrape_fanqie_rank)
                                  ↓ (rank_scraped 信号)
                                _on_fanqie_rank_scraped:
                                  - 0 条 → fallback 旧 prompt
                                  - 正常 → filter_by_genres → build_enhanced_prompt → 发 AI
  ```

### BUG-086 第二组:钩子检测松绑(修第 1 章死磕 10 次)

- **实战触发**(用户日志):第 1 章死磕 10 次全失败 → 浪费 ~7 分钟 + 大量 token。
  AI 文风评分一直 8.0/10,问题在钩子检测:章里其实有"倒计时 ★★★★★"钩子已入库,
  写后校验却一直说"末段无悬念"。
- **根因**:**两个判定器走两条独立路径,标准不一致**:
  - **钩子入库**:解析 AI 输出的 `【断章钩子】` 元信息块(类型/强度/内容)→ 写入 chapter dict
  - **钩子检测**:`HOOK_MARKERS` 80+ 关键词扫描末段 300 字 — 但**倒计时类钩子 "还有 7 天"
    不在关键词列表里**,所以 AI 明明写了强钩子,检测却失败
- **修法**(最小化改动,不动函数签名):
  1. 加 `_extract_hook_intensity_from_text(content)` helper:从内容末 2000 字解析
     `【断章钩子】` 块,统计 `强度:` 行 ★ 数量,返回 0-5
  2. `_check_chapter_quality` 钩子检测:HOOK_MARKERS 失败时调 helper,若 ≥ 4 星
     → **放行 + 日志说明**(`[BUG-086]` 标识)
- **设计取舍**:
  - 为什么 4 星阈值:5 星太严苛(AI 偶尔自谦写 4★);< 4 星实战经常是 "弱钩子" 误标;
    4 星是"AI 明确说这是强钩子"的最低线
  - 为什么放行不重写关键词列表:HOOK_MARKERS 一旦扩太宽会让"无钩子章节"也过关
    (反向回归)。元信息里 AI 自己声明 ★★★★+ 是更可靠的信号
  - 为什么不动死磕次数(用户原议 "5 次后放行"):死磕次数松绑挪到上层 `_retry_chapter_with_reasons`,
    已经有 `retry <= 0 → 接受这章` 逻辑;关键是先解决"为啥误判",而不是"误判后放手"

### 改动汇总

| 文件 | 改动 |
|---|---|
| `core/fanqie_rank_scraper.py` | **新增** 460+ 行 — 纯逻辑模块,扫榜数据结构 + 缓存 + 题材匹配 + 卖点提炼 + 增强 prompt 构造 |
| `ui/browser_worker.py` | +131 行 — rank_scraped 信号 + scrape_fanqie_rank action + _scrape_fanqie_rank 方法(navigate + 滚动 + JS 多重 selector 抓取 + 失败兜底) |
| `novel_ai.py` | +135/-7 行 — gen_inspiration 分支(扫榜/fallback) + _on_fanqie_rank_scraped slot + 三个辅助方法 + _extract_hook_intensity_from_text helper + _check_chapter_quality 强度松绑 + connect rank_scraped + APP_VERSION → v2.23.0 |
| `test_bug086_v23_scrape_rank_hook_relax.py` | **新增** 31 守护测试(纯逻辑层 + 集成静态检查) |
| `项目对接记忆.md` | 加本章节 + commit 表 + 当前版本 + 签名 |

### 测试零退化

**228 passed**(197 既有 + 31 新)。scraper 模块 16 条纯逻辑测试 + worker 集成 4 条 +
主进程集成 5 条 + 钩子松绑 4 条 + 版号守护 1 条 + 模块结构 1 条。

### 下一代 Claude 提醒

1. **纯逻辑跟 Qt 解耦的模式有用** — `core/fanqie_rank_scraper.py` 不 import Qt,可以
   纯 pytest 单测(16 条直接覆盖)。以后做新功能(榜单 / 风格库 / 卖点分析)都该走这模式:
   把数据结构和算法塞 `core/`,让 `ui/` 只管 Qt 信号槽。

2. **新爬虫 selector 必带多重候选 + 启发式兜底** — `FANQIE_SCRAPER_PROFILE` 里
   `book_card_selectors` 有 3 个候选,JS 里还有启发式("找 a[href*='/page/'] 然后向上找父容器")。
   番茄随时可能改 DOM,无候选 = 一次升级就崩。**所有第三方网站抓取都该这么做。**

3. **跨进程信号传对象用 dict 不传自定义类** — `rank_scraped = pyqtSignal(str, list)`
   传的是 `list[dict]` 不是 `list[Book]`。Qt 跨线程信号有序列化问题,自定义类
   (尤其有 __slots__ 的)经常出问题。规则:**信号载荷只用 str/int/dict/list 基本类型**。

4. **元信息 vs 关键词检测的双判定不一致是常见坑** — BUG-086 钩子检测就是这样
   摔的:入库走元信息块,检测走关键词。下次再加"AI 输出后校验"功能时,**先想清楚
   "我用元信息当事实,还是用文本扫描当事实"**,只选一个,或者两个都用但要明确
   优先级 + 冲突时谁说了算。

5. **缓存键要 sorted(genres)** — `FanqieRankCache._key` 用 `tuple(sorted(genres))`
   而不是 `tuple(genres)`,这样用户选 ["玄幻","重生"] 和 ["重生","玄幻"] 命中同一
   缓存。**所有"集合作为键"的场景都该 sort,否则等同于失效缓存。**

### 已知 / 未做

- **风格库不管用**(用户上轮反馈):章节生成 prompt 没注入风格库匹配结果,匹配
  只是装饰品。**留下一轮单独做**(走 B 方向:加风格下拉让用户选)
- **多平台扫榜**(七猫 / 起点 / 晋江):本轮只做番茄。架构上 scraper profile 是
  per-site 字段,加新平台只需新建 profile + 改 platform 判断
- **历史榜单趋势**:能存最近 7/30 天扫到的榜单做趋势分析,挖"新冒头题材"。
  以后做

---

**v2.22.3 BUG-084/085 详情(下面是上一个里程碑)**:

### BUG-084:BUG-082 守卫死循环(3 分 35 秒 200+ 圈)

- **实战触发**(2026-05-26 21:45:27 → 21:49:01,v2.22.2 BUG-083 已修后):
  ```
  [21:45:27] · [BUG-082] 按钮恢复但 67 字 < 站点下限 100 → 继续等 Qwen 流式输出...
  [21:45:27] ⏳ 按钮快照已恢复(可能写完),0.8s 保险确认...
  [21:45:28] ⏳ 按钮快照已恢复(可能写完),0.8s 保险确认...
  ... (持续刷屏 200+ 行,3 分 35 秒)
  ```
  期间 `AI 生成中...已 Xs,当前 N 字符` 这条**彻底消失**。polling 陷入死循环。
- **根因**:BUG-082 守卫(C/B 兜底)`continue` 后,外层 while 立刻又触发"按钮恢复"
  判定 — Qwen 的 67 字思考预览顶着按钮恢复 + cur 不变,所以"⏳ 按钮快照已恢复"
  又命中,刷日志,sleep 0.8s,recheck 还是 67 字,守卫又拒绝完成,continue 回顶。
  每秒循环 ~1 圈,3.5 分钟跑 ~250 圈。
- **修法**(三条防御):
  1. **守卫累计超时 `_B082_GUARD_MAX = 90.0s`**:第一次守卫触发开始计时,90 秒
     还在守卫拒绝 → 放弃守卫,接受当前内容(67 字也比死循环好,上层会重试)
  2. **守卫触发后 sleep 3 秒**:避免紧循环抢 CPU + 给 Qwen 真出字时间
  3. **日志抑制**:"⏳ 按钮快照已恢复" 在守卫触发后只刷第一次

  **字符真涨时重置守卫计时 + 解除日志抑制**,避免多个守卫窗口串扰。

### BUG-085:Qwen 思考期 `_check_timeout` 误报"卡住"

- **实战触发**(2026-05-26 21:44:11,任务实际 17 秒后成功 985 字):
  ```
  [21:44:11] ⏰ 「Canon抽取-第1章」已等待 90 秒,0 字节无回复(可能真卡住了)!
  ```
  弹窗 + TTS 报警。但实际上任务没卡 — Qwen 还在思考阶段(thinking_indicator 亮),
  正常 0 字节,17 秒后开始流式输出 985 字成功完成。
- **根因**:v2.22.2 BUG-083 修复用 `字符进度 == 0 → 报警`,但 **Qwen 思考阶段
  就是合法 0 字节 0-90 秒**,需要更细的"思考期"维度。
- **修法**(信号驱动,worker → 主进程):
  1. **worker 加 `task_thinking = pyqtSignal(str, bool)` 信号**
  2. **polling 每圈检测 thinking 状态变化**(`_last_thinking_emitted` 缓存)— 变化才 emit
  3. **polling 退出前 emit `False`** — 避免任务完成后还以为在思考
  4. **主进程 `_task_thinking_state: dict` 跟踪每个任务**
  5. **`_check_timeout` 加思考期闸门**:thinking=True → 延期 60 秒再查;
     thinking=False + 0 字节 → 真卡了报警
  6. **`_on_response_received` 清理** thinking_state

### 改动汇总

| 文件 | 改动 |
|---|---|
| `ui/browser_worker.py` | +115/-1:task_thinking 信号 + BUG-084 三条守卫 + BUG-085 每圈 emit + 退出 emit False |
| `novel_ai.py` | +41/-3:connect + slot + state dict + _check_timeout 闸门 + pop 清理 + APP_VERSION |
| `test_bug084_bug085_qwen_thinking_loop.py` | **新增** 17 守护测试 |
| `test_bug082_qwen_stream_polling.py` | +12/-2:`test_min_chars_blocks_premature_completion` 放宽 — 允许 `_B082_GUARD_MAX` 或 `[BUG-084]` 标识的守卫超时分支 break(BUG-084 新设计) |
| `项目对接记忆.md` | 加本章节 + commit 表 + 当前版本 + 签名 |

### 测试零退化

**197 passed**(180 既有 + 17 新),BUG-08x 系列全过(BUG-076/081/082/083/084/085 共 6 个修复全部守住)。

### 守护测试 17 条

**BUG-084(7)**:guard_first_at 变量、`_B082_GUARD_MAX` 在 60-120s、`_b082_guard_sleep ≥ 1.0`、`⏳` 日志被 silenced 抑制、`[BUG-084]` 标签存在、守卫超时分支必有 break、guard_first_at 至少出现 2 次(初始化 + 重置)

**BUG-085(9)**:task_thinking 信号(str,bool)、主进程 connect、`_on_task_thinking` 方法、`_task_thinking_state` dict、`_check_timeout` 查 thinking_state + QTimer.singleShot 延期、`_on_response_received` pop 清理、worker emit + `_last_thinking_emitted` 缓存、退出前 emit False、APP_VERSION == v2.22.3

**综合(1)**:旧的"⏳" log_signal 调用必须有抑制保护

### 为什么 BUG-084/085 合并 v2.22.3

两个 bug 都跟 **Qwen 思考阶段 × 旧修复的交互**有关,根因在同一段代码周边(polling
循环 + _check_timeout)。一次性修干净:一个 commit、一个 patch、一次记忆更新;
守护测试共享文件;避免修了 BUG-084 才发现 BUG-085 的"半修复状态"。

### 下一代 Claude 提醒

1. **守卫机制必带"累计超时"**:BUG-082 教会"加守卫防误判",BUG-084 教会"守卫
   本身也要能放弃"。任何 `continue 跳 break` 的守卫,都要有 `_GUARD_MAX` 累计超时
   托底,否则就是潜在死循环。
2. **守卫 continue 前必 sleep**:`continue` 不带 sleep 等于让 polling 紧循环抢 CPU
   + 刷日志。最少 1 秒,理想 3 秒。
3. **错误信号必带日志抑制**:每圈都打的日志,死循环时会刷 200+ 行淹没现场。每条
   循环里的 log_signal 都该问:"这条会被死循环触发吗?" 会就加 silenced 标记。
4. **`worker → 主进程` 进度信号是通用模式**:BUG-083 加 `task_progress`,BUG-085
   加 `task_thinking`,以后做"实时进度条"、"卡死自动重启"都可以加新信号。push
   模式比主进程 pull 高效百倍。
5. **任务完成清字典是铁律**:`_on_response_received` 必须 pop 所有任务相关状态字典,
   否则跑长批量任务字典会无限增长。

---

**v2.22.2 BUG-083 详情(下面是上一个里程碑)**:

- **实战触发**(2026-05-26 用户反馈):
  ```
  [19:32:41] ⏰ 「第 1 章」已等待90秒未回复!
  ```
  但实际上 AI 正在写章节,字符数从 0 → 5126 字一直在涨。Qwen 写章节本来就要
  4-5 分钟,90 秒时远没完成,**每次都误报弹窗**骚扰用户。
- **根因**:`novel_ai.py` 第 1786 行 `QTimer.singleShot(90000, _check_timeout)`
  是**纯时间触发**:90 秒到了,只要任务还在 `_pending_task_targets` 字典里(没完成),
  就弹"⏰ 任务超时"。完全不看 AI 有没有在写字。对 DeepSeek 这种快的 AI 可能勉强能用,
  对 Qwen / 长章节(任何 > 90s 的任务)就是无差别误报。
- **修法**(信号驱动,worker → 主进程):
  1. **worker 加 `task_progress = pyqtSignal(str, int)` 信号** — task_id + 当前字符数
  2. **polling 循环里 emit** — 两处:内容变化时 + 每 5 秒同步一次
  3. **主进程加 `_task_char_progress: dict[task_id, int]`** 跟踪每个任务的字符数
  4. **`_on_task_progress` slot** 接收 worker 的进度更新
  5. **改 `_check_timeout` 逻辑**:90 秒到了去查 `_task_char_progress`,
     - 字符数 == 0 → 真卡了 → 弹窗 + TTS(原行为,但只在这种情况触发)
     - 字符数 > 0 → AI 正在写 → 静默 return,不打扰
  6. **`_on_response_received` 清理**:任务完成时 `pop(task_id)`,防内存泄漏
- **报警文案**也改了 — 加上"0 字节"字眼让用户清楚为什么报警:
  - 旧:`已等待 90 秒没有回复`
  - 新:`已等待 90 秒,0 字节无回复(可能真卡住了)`
- **改动汇总**(总 ~75 行):
  - `ui/browser_worker.py`:+1 行 signal 定义 + 注释 + 两处 emit(共 +18 行)
  - `novel_ai.py`:+1 行 signal 连接 + `_on_task_progress` 方法(+19 行)+
    `_check_timeout` 改造(替换 23 行 → 38 行)+ `_on_response_received` 清理(+3 行)
  - `APP_VERSION`:v2.22.1 → **v2.22.2**
  - 新增 `test_bug083_stuck_alert_zero_byte.py`(9 测试)
- **测试零退化**:**284 passed**(275 既有 + 9 BUG-083 新)。
- **守护测试 9 条**(`test_bug083_stuck_alert_zero_byte.py`):
  1. worker 有 `task_progress = pyqtSignal(str, int)` 信号
  2. polling 循环里至少 emit 2 次 task_progress(内容变化 + 5s 同步)
  3. 主进程必须 `task_progress.connect(self._on_task_progress)`
  4. `_on_task_progress` 必须是方法且接受 (task_id, char_count) 参数
  5. `_check_timeout` 必须查 `_task_char_progress` 且有 `char_progress > 0 → return` 判定
  6. `_on_response_received` 必须 pop 当前 task_id 的进度记录(防内存泄漏)
  7. 报警文案必须含"0 字节"(跟旧文案区分)
  8. APP_VERSION ≥ v2.22.2
  9. 两个文件都必须有 BUG-083 标识

**为什么不直接删 `_check_timeout`**:有些用户依赖这个"卡死告警"。worker 那边只在
内部 log,没弹窗 + TTS。删了主进程报警 = 真卡死了用户不知道。所以保留报警机制,
只改成"字符数为 0 才触发"。

**下一代 Claude 提醒**:
1. **新加"超时类报警"机制时,默认要带"进度"维度**,不能纯时间触发。
   时间触发对快任务可以,对慢任务(Qwen / 长章节 / 思考型 AI)就是无差别骚扰。
2. **worker → 主进程的进度上报是个通用模式**,以后做"实时进度条"、"卡死自动重启"、
   "动态超时延长"都可以复用 `task_progress` 信号 + `_task_char_progress` dict。
3. **任务完成时清字典 / list 是铁律**,否则跑长批量任务字典会无限增长。这次
   `_on_response_received` 里加的 pop 就是这个习惯。守护测试 #6 防回归。

---

**v2.22.1 BUG-082 详情(下面是上一个里程碑)**:

- **实战触发**(2026-05-26 19:34,v2.22.0 BUG-081 已修后,跑 Canon 抽取):
  ```
  [19:33:04] 提示词已发送
  [19:33:25 ~ 19:34:41] AI 生成中...已 5s..80s,当前 0 字符  ← Qwen 思考 80s 不吐字
  [19:34:45] AI 生成中...已 85s,当前 12 字符
  [19:34:45] AI 生成中...已 85s,当前 17 字符
  [19:34:47] ✓ 内容稳定 0.9s → 完成 (17 字符)               ← 误判!
  [19:34:47] 原始前 200 字:[{"key":"角色.苏棠.体质           ← JSON 才开头就被截
  ```
- **根因**:Qwen 流式输出比 DeepSeek 慢 5-10 倍 — prompt 后会先有 5-90s"思考"
  阶段然后才开始逐字符吐出,且**单字符间隔可能 > 1s**。Polling 的三档稳定阈值
  (`< 300 字 → 0.9s ultrafast`)对 DeepSeek 完美(每秒几十字),对 Qwen 灾难 —
  17 字时字符间隔超过 0.9s,polling 误判"内容稳定 = 完成"。
- **跟 BUG-081 的关系**:BUG-081 修了 Qwen 的"发送按钮检测",这次实战日志确认
  `按钮态=enabled:site_profile`(我加的标识符)真生效了,Qwen 摘要任务从"三次
  重试全挂走本地降级"变成"一次重试就成功 1749 字"。BUG-081 收工,BUG-082 是
  下一个独立 bug。
- **修法**(三层防御,**用户给出的 DOM 信号是关键**):

  **C. thinking_indicator(主防御 — 用户实地侦察后给出的 DOM 信号)**:
    用户实地查 Qwen 页面,提供了"思考中"和"已完成思考"两种状态的 HTML:
    - 思考中:`<div class="...qwen-chat-status-card-title-animate" ...>梳理情节脉络...</div>`
    - 完成后:`<div class="...qwen-chat-thinking-status-card-completed ...">已经完成思考</div>`

    关键区别就是 `-animate` 后缀的动画 class。修法:Qwen profile 加
    `thinking_indicator: ".qwen-chat-status-card-title-animate"`,polling 调用
    `_site_is_thinking(selector)` 检查 DOM 里有没有这个元素。**有就是还在
    思考,直接跳过完成判定**。这是**确定性信号**,不靠时间/字数估算。

  **A. stable_wait_min=8.0(兜底)**:万一未来 Qwen 改 class 名,C 失效,A 把
    完成等待时间提到 8 秒(原 ultrafast 0.9s)。`max(原三档阈值, 站点下限)`。

  **B. min_complete_chars=100(兜底)**:字符数 < 100 强制不完成。JSON 短输出
    的最小合理长度(摘要/Canon/角色库实战合法输出都 ≥ 几百字)。

  **优先级**:在两条完成路径(按钮快照恢复 / 内容稳定)的 break 前依次检查
  `C → B → 完成`。C 是确定性信号最优先,B 是兜底其次。
- **DeepSeek 等不设这三字段** → `prof.get(...)` 返回 None → polling 不触发任何
  下限逻辑 → **所有非 Qwen 站点行为完全不变**。
- **改动汇总**(总 ~80 行,纯加字段 + 加 helper + 加守卫,不改原逻辑语义):
  - `core/site_profiles.py`:Qwen profile +3 字段 + 详细注释 +28 行
  - `ui/browser_worker.py`:新加 `_site_is_thinking()` helper(+25 行)+ polling
    循环里读 3 字段(+15 行)+ 两条完成路径加 C→B 链式守卫(+24 行)
  - `novel_ai.py`:APP_VERSION v2.22.0 → **v2.22.1**
  - 新增 `test_bug082_qwen_stream_polling.py`(14 测试)
- **守护测试 14 条**(`test_bug082_qwen_stream_polling.py`):
  1. Qwen profile 有 thinking_indicator 字段(非空字符串,含 "animate")
  2. Qwen profile 有 stable_wait_min ≥ 8.0
  3. Qwen profile 有 min_complete_chars ≥ 100
  4. DeepSeek profile 这 3 字段都为 None(防误伤其它站点)
  5. polling 必须读这 3 个字段
  6. `_site_is_thinking` helper 必须存在(AST 检查 + 接受 selector 参数)
  7. `_site_is_thinking` 至少被调用 3 次(定义 + 两条完成路径)
  8. browser_worker 至少 4 处 BUG-082 标识
  9. `stable_wait_min` 用 `max(原阈值, 站点下限)` 不能替换
  10. **C 守卫必须在 B 守卫前面**(确定性信号优先)— 在内容稳定路径里 AST 检查
  11. min_chars 守卫必须 `continue`/`pass` 不 `break`
  12. APP_VERSION ≥ v2.22.1
  13. `_site_min_chars` 至少出现 3 次
  14. `_b082_thinking_warned` 标志位防刷屏
- **测试零退化**:**275 passed**(261 既有 + 14 BUG-082 新),Python AST +
  py_compile 双过。

**为什么用户给的 DOM 信号 > 我原方案的 A+B**:
我原本设计的是"用 8.0s + 100 字估算 Qwen 写完了没"。用户实地侦察后提供了
Qwen UI 的确定性 thinking-status 信号 — 这是 Qwen 自己说"我思考完了"的信号,
比任何时间/字数估算都精确。决策路径:
- 用户没给信号时 → 只能 A+B 估算,**会有误判风险**
- 用户给了信号 → C 作主防御直接确定,A+B 退化为兜底

**这教训是**:遇到"自动化判定不准"的问题,第一选择应该是**找站点 UI 提供的
显式信号**,而不是堆估算阈值。下一代 Claude 接其它站点遇到类似问题时:**先
问用户能不能给你"AI 正在思考"和"思考完了"两种状态的 HTML**。

**为什么本轮一并升 v2.22.1 而不是先推 v2.22.0 chore 再推 v2.22.1**:
v2.22.0(BUG-081 修复)推的时候 APP_VERSION 没同步升,记忆也没同步。这次 BUG-082
正好做收尾 + 新修复,合并成一个 v2.22.1 commit。

**下一代 Claude 提醒**:
1. **新站点 profile 加完后,如果该站有"思考模式"UI,问用户要"思考中"和"思考完"
   两种状态的 HTML 片段**,然后填 `thinking_indicator` 字段。这比堆 stable_wait
   阈值精确得多。
2. **`thinking_indicator` 字段是站点级的,只对"该站确实有这种 UI"的站点设**。
   DeepSeek / 豆包 / Gemini 等流式快的不设,行为不变。
3. **三层守卫优先级 C > B 必须保持**。如果未来要重构,记得 C 必须在 B 之前
   检查 — 守护测试 #10 (`test_thinking_guard_priority_over_chars_guard`) 会
   catch 这条反转。
4. **BUG-082 不在 `DEFENSE_FINGERPRINTS` 字典里**。这是"配置类"bug,核心修复
   就是 profile 那三行 + polling 那 ~60 行守卫。靠 14 个守护测试就够。

---

**v2.22.0 BUG-081 详情(下面是上一个里程碑)**:
- 实战触发(2026-05-26 19:12,用户跑章节摘要时,Qwen 标签):
  ```
  [19:12:42] ⚠ Enter 后 5s 仍未确认发送(0→0 / textarea 未清空 / 按钮态=none:no_btn_in_dom)
  [19:12:42] ℹ 🤖 RL 建议跳过策略 B(避免误点 stop),return False
  [19:12:53] ⚠ ⚠ 关键任务[chapter_summary]重试 2 次仍失败,启用本地降级(723 字),避免数据丢失
  ```
- **根因**:`ui/browser_worker.py` 里 `_get_send_button_state` 和 `_dispatch_send` 策略 B 的 JS 用的是**通用 selector 兜底**(`button.composer-submit-btn` / `[data-testid="send-button"]` / `aria-label*="发送"`),但 Qwen 的发送按钮是 `button.send-button` —— 这套通用兜底**全部不命中**,DOM 里明明有按钮却返回 `no_btn_in_dom`。RL 看到"按钮态=none"以为是页面状态异常,建议跳过策略 B 防止误点 stop,于是 textarea 注入 + Enter 也失败、按钮策略也跳过 → 三次重试全挂 → 走本地降级。
- **修法**(`ui/browser_worker.py` 两处 JS,共 +55 行 / -4 行):
  - 在 JS 最前面**先用站点 profile 的 `send_btn` selector 试一次**:`SITE_PROFILES['chat.qwen.ai']['send_btn'] = 'button.send-button'`(项目里早就配了,只是 JS 没用上)
  - 通过 `_dispatch_send` / `_get_send_button_state` 在 selenium 端先 `_profile_for_url(url)['send_btn']`,把字符串作为参数注入 JS,JS 里 `if (siteSel) { btn = document.querySelector(siteSel); if (btn) return ... }`,失败再走通用兜底
  - **关键**:不替换通用兜底,只在前面叠加站点专用 selector —— DeepSeek / 豆包 / 元宝 等其它站点行为完全不变
- **测试零退化**:既有 BUG 套件 157 + main_p2p3 + research_skills 共 **177 测试全过**,Python 语法 / py_compile / AST 三道检查全过
- **守护测试**:本次没新增专用 test 文件(逻辑只在 JS 里,Python 侧难直接测),靠"既有 177 测试不退化"+ 用户实战回归
- **APP_VERSION**:v2.21.5 → **v2.22.0**(主版号升,因为这条修复直接影响"主 AI + 副 AI 双 AI 工作流"的可用性,比 patch 级别重)
- **commit**:`f37accf feat v2.22.0: BUG-081 Qwen send button 检测失败修复`(已真推上 master,三路独立验证:`git ls-remote` / REST API / raw CDN 全部一致)

**为什么 BUG-081 不像 BUG-074/-076 那样有专用测试文件**:JS 字符串拼接不在 Python AST 可见范围内,做静态守护要么误报多要么覆盖不到。**取舍**:既有 177 测试零退化 + 用户实战验证比写一个易碎的 JS 字符串 grep 测试可靠。

**下一代 Claude 提醒**:
1. **每加一个新站点 profile,JS 端的按钮检测也要确认能用上站点 `send_btn` selector**。`SITE_PROFILES` 里配了选择器是一回事,JS 真去用是另一回事 — BUG-081 暴露的就是这个"配了没用上"的 gap。
2. **RL 决策跳过策略 B 是合理的安全机制,不要去删它**。原代码里 `RL 建议跳过策略 B(避免误点 stop)` 是为了防止在不确定按钮态时误点停止按钮,这个保护是对的。BUG-081 不是 RL 的错 — RL 是基于"按钮态=none"做的合理决策,根因在"按钮态为什么是 none"(检测漏)。
3. **历史债务**:记忆里 v2.12 → v2.21.5 之间 12+ 个版本没补到记忆(双 AI 分工、关系图、伏笔检查独立 Tab、智能冷却、隐藏浏览器、自动关机、4K 适配等)。**这是上一代我违反了"每 push 必同步记忆"的铁律**。下一代 Claude 如果有时间,可以读 `git log --oneline f37accf...368c0bd` 把这些里程碑补到记忆中间;如果没时间,至少保持 v2.22.0 之后的记忆同步。

---

**v2.11 BUG-076 详情(下面是上一个里程碑)**:

**实战触发**(2026-05-22 14:37:40,用户跑批量提取角色库时):
```
[14:37:40] ✗ 任务执行失败:name '_profile_for_url' is not defined
```

**根因**:**P6 拆分时(v2.05 `818d814`)AST 抽取脚本漏识别了 module-level 函数依赖**。BrowserWorker 在 `_dispatch_send` / `_send_prompt` / `_build_send_xpath` 等 **4 处**用 `_profile_for_url`,但 P6 把 BrowserWorker 从 novel_ai.py 迁到 `ui/browser_worker.py` 时只补了 class/method 依赖,没补 module-level 函数依赖。

**为什么 1223 测试没抓到**:这些调用都在运行时真实派任务路径(`_dispatch_send` 等),静态扫描进不去。test_full_integration 实例化 MainWindow 也不会触发(它只测构造 + 信号连接,不真发 prompt)。

**修法**:`ui/browser_worker.py` 顶部加一行:
```python
from core.site_profiles import _profile_for_url
```

**跟 BUG-074 同根因**(P3~P6 拆分留下的隐藏地雷)。BUG-074 是 PyQt5 类没 import(`Qt`/`QFont` 等),BUG-076 是 module-level 函数没 import,都因 AST 抽取脚本"外部依赖识别"覆盖不全。

**守护测试**(`test_bug076_browser_worker_profile_import.py`,5 个):
1. `test_can_import_from_browser_worker`:`ui.browser_worker` 真实 import `_profile_for_url` 不挂
2. `test_function_actually_works`:实际调用 `_profile_for_url('https://chat.deepseek.com/')` 返回 profile dict
3. `test_top_level_import_present`:AST 检查顶部 ImportFrom 节点真有这个 import
4. `test_profile_for_url_imported_wherever_used`:**通用守护**——扫所有 `ui/*.py` + `ui/tabs/*.py`,只要用了 `_profile_for_url` 名字就必须顶层 import(防止下次又漏)
5. `test_at_least_v211`:APP_VERSION 守护

**工程取舍记录**:
我一开始尝试写"通用 NameError 巡检"(扫所有 ui/ 模块,每个函数体内的裸名跟模块顶层符号比对),失败了——comprehension target 拆包 / 多层作用域 / 动态属性 都要正确处理,误报 1101+ 个。**正确取舍**:不做不可靠的通用巡检,做精准的"已知问题守护"。如果未来又出 BUG-077 风格的 import 漏,加一个新文件 + 一个新的"全 ui/ 用了 X 必须 import X"测试即可——5 行代码,可维护。

**改动汇总**:
- `ui/browser_worker.py`:+5 行(1 行 import + 4 行注释)
- `novel_ai.py`:APP_VERSION v2.10 → **v2.11**
- 新增 `test_bug076_browser_worker_profile_import.py`(255 行 / 5 测试)
- 修改 `项目对接记忆.md`(顶部 v2.11 章节 + 版本号铁律段 + 末尾签名)

**测试零退化**:**251 passed**(BUG 套件 + housekeeper P1/P2/P3 + main p2p3 集成 + full_integration 48 + BUG-076 新 5 个)

**前一个里程碑**:`184c81e` — v2.10 主程序集成 P2/P3 hooks(终于让能力真起作用)。

---

### v2.10 详情(2026-05-22,Task 3 最后一步)

**远端 HEAD**:`184c81e` — **v2.10 🔌 主程序集成 P2/P3 hooks(终于让能力真起作用)**。

把 v2.08(P2)+ v2.09(P3)留在 housekeeper.py 的 5 个 hook 真正接到主程序里。**这是 v2.08~v2.09"零侵入设计"的对偶动作**:能力已经准备好,这一轮把开关合上。

**改了什么**:

1. **`MainWindow.__init__` 末尾**:`hk.set_rl_reward_callback(self._on_hk_health_to_rl)`(P3-#10)
2. **新方法 `_on_hk_health_to_rl(score, report)`**:MVP 仅写日志("第N章健康度→RL 反馈通道:🟢90%"),**没真喂 flow_rl**(理由见下文)
3. **新模块级常量 `DEFENSE_FINGERPRINTS`**:8 个历史 BUG 的指纹字典(BUG-028 / -065 / -066 / -067 / -068 / -069 / -071 / -073),共 13 条 pattern,全部经过真实存在性验证
4. **`_accept_chapter_and_continue` finalize 前**:
   - `hk.record_canon_locked_mismatch(...)` — 遍历 `tab_canon.parse()` 的 high 严重度 locked 项,value 不在本章正文 → record(P2-#6)
   - `hk.verify_defenses(DEFENSE_FINGERPRINTS, [novel_ai + ui/* + core/*])` — 每 10 章触发(P3-#12)
5. **`_accept_chapter_and_continue` finalize 后**:`hk.check_pacing_window(n=5)` — 每 5 章触发,检测到疲软 log 到生成 Tab(P2-#7)

**未集成的(留待后续)**:
   - `snapshot_for_recovery(project_root, ch_num)` — 本项目用 single json 文件存档,跟 `project_root` 目录概念不匹配。要做的话或者改用我 BUG-075 设计的 `make_backup(data dict)` 风格,或者重构存档结构。**这一轮不动**

**关键设计:RL 反馈联动 MVP 只记录不真喂 flow_rl 的理由**:
   - flow_rl 的 state/action 是浏览器决策粒度(按钮态/重试策略),跟"章节级整体健康度"不直接对应
   - 没 choose_action 配对的 reward 调用,会在 Q 表留孤儿条目
   - 健康度的"赋分公式"(score → reward value)是产品决策,不该由 hook 默认设
   - **用户/下一代 Claude 想真接,在 `_on_hk_health_to_rl` 方法体内补 5 行即可**(注释里有现成示例代码)

**改动汇总**:
   - `novel_ai.py`:9653 → 9782 行(+129),APP_VERSION v2.09 → **v2.10**
     - DEFENSE_FINGERPRINTS 字典 +36 行(含编码原则注释)
     - init 末尾 RL 回调注册 +6 行
     - 新方法 _on_hk_health_to_rl +37 行(含详尽 docstring + 现成示例代码)
     - _accept_chapter_and_continue 集成块 +50 行(3 个 hook)
   - 新增 `test_main_p2p3_integration.py`(20 测试:1 APP_VERSION + 3 字典 + 3 RL callback + 2 canon locked + 3 verify_defenses + 3 pacing + 4 不破坏既有 + 1 v2.10 标识)
   - **零侵入设计反过来:这一轮真改了主程序逻辑,把所有改动用 `try/except + HOUSEKEEPER_AVAILABLE` 双重保护**,housekeeper 缺失时主程序仍正常运行

**测试零退化**:**1223 passed**(1010 pytest + 213 自跑式)。
   - `test_main_p2p3_integration.py` 新 20 个全过 ✅
   - `test_full_integration.py` 48 测试全过(**实际实例化 MainWindow,证明 init 中的 RL 回调注册真能跑**)
   - 既有 housekeeper P1+P2+P3 全部 81 测试不退化(19+23+39)
   - BUG-065~075 套件 180 测试全过

**关键收获(给下一代 Claude)**:
   - **8 个 BUG 指纹的字典是知识沉淀**:看 DEFENSE_FINGERPRINTS 一眼就知道"哪些 BUG 是绝不能回退的工程级防御点"。比读 6102 行的 docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md 高效十倍
   - **添加新指纹的流程**(主程序注释里已写):修完一个真正棘手的 BUG 后,在字典里加 `"BUG-XXX": [独特模式 1-3 个]`,选具有特征性的标识符,避免常见词
   - **指纹真实性自检**:`test_all_fingerprints_real` 会扫主程序 + ui/ + core/,验证每条指纹真存在。**这条测试本身是个"二道闸"**——未来重构若误删某 BUG 的关键代码,这个测试立刻报警
   - **MVP 路径很重要**:`_on_hk_health_to_rl` 不真接 flow_rl,看似"没完成",其实是个**正确决策**:让框架先到位,具体公式让用户实战决定。强行决定 reward 转换公式会污染 RL 状态

**前一个里程碑**:`395ab83` — v2.09 Housekeeper P3 两扩展点(Task 3 接续)。

---

### v2.09 详情(2026-05-22,Task 3 接续)

**远端 HEAD**:`395ab83` — **v2.09 ✨ Housekeeper P3 两扩展点(Task 3 接续)**。

实现 v1.90 P1 管家末尾 + v2.08 P2 末尾留的 2 个 P3 扩展点(零侵入,跟 P2 同模式)。

**改了什么**:

1. **`set_rl_reward_callback(callback)`** — RL 反馈联动(P3-#10)
   - housekeeper 加 `rl_reward_callback` 实例属性 + setter 方法
   - `finalize_chapter` 末尾自动 `callback(health_score, report_dict)` 触发
   - **housekeeper 不 import flow_rl**(零依赖,完全解耦)— 用 AST 测试强制约束
   - 调用方写 closure 把 health_score 转 reward 值,连接到自己持有的 FlowRL 实例
   - 非 callable 静默拒绝(符合容错风格),callback 抛异常吞掉不影响 finalize 返回

2. **`verify_defenses(fingerprints, source_paths)`** — 二道闸巡查(P3-#12)
   - 传入 `{bug_id: [code_pattern, ...]}` 指纹字典 + 源码路径列表
   - 对每个 BUG,**所有 pattern 都必须出现**才算"防御完好"
   - 失配 → 加到 `current_report.missing_defenses`(去重) + 返回 `{bid: False}`
   - 源码文件读不到 → **保守视为 False**(防御消失)
   - 默认 source_paths = `["novel_ai.py"]`,扩展场景可加 ui/ 文件

**HousekeeperReport 加字段**:
   - `missing_defenses: List[str]`(失配的 BUG ID 列表)
   - 健康度算法新加 `defense_penalty = min(0.4, 0.15 * len(missing_defenses))`
     —— **比 lock_penalty(0.1/条)严厉**:locked 是数据写错,defense 消失是工程级回归(更难发现)

**oneliner 加 🛡️ 标**:
   - 显示 `🛡️ 防御消失:BUG-A/BUG-B (+N)`(前 2 个 BUG + 多余 count)
   - **修正了一个潜在的重复显示**:`_record_missing_defense` 不再发 warning,
     因为 🛡️ 区已经显示 + defense_penalty 已经严厉扣分,
     再发 warning 会让 oneliner 出现"⚠ XXX 防御消失 | 🛡️ XXX"两遍

**设计原则**(同 v1.90/v2.08):
   - 零侵入:只动 housekeeper.py,主程序不需要改
   - 失败容错:try/except 全包,主流程零干扰
   - 单测友好:不依赖 PyQt5

**改动汇总**:
   - `housekeeper.py`:571 → 740 行(+169),2 个新方法 + missing_defenses 字段 + oneliner 🛡️ 标 + 健康度 defense_penalty + finalize_chapter 末尾 emit + __init__ 加 rl_reward_callback + typing 加 Callable
   - 新增 `test_housekeeper_p3.py`(39 测试:2 字段 + 5 健康度 + 4 oneliner + 10 RL callback + 11 verify_defenses + 5 不破坏既有 + 2 APP_VERSION)
   - `novel_ai.py`:APP_VERSION v2.08 → **v2.09**

**测试零退化**:**1203 passed**(990 pytest + 213 自跑式),全 49 个测试文件 100% 通过。

**主程序集成留待用户决定**(同 v2.08 风格):2 个 P3 方法已实现 + 测试覆盖,但主程序 _accept_chapter_and_continue 没主动调用。调用方法:
   - `hk.set_rl_reward_callback(fn)`:MainWindow.__init__ 调一次,fn 内写 `flow_rl.reward(...)` 即可
   - `hk.verify_defenses(FINGERPRINTS, [...])`:在 finalize_chapter 之前调,或者每 N 章触发一次

用户可下一轮决定接入(或者完全不接入,housekeeper 仅提供能力)。

**指纹字典编码示例**(给下一代 Claude 参考,实战时填进 novel_ai.py 主程序):
```python
DEFENSE_FINGERPRINTS = {
    "BUG-028": ["_chapter_fingerprint", "已生成过该指纹"],
    "BUG-065": ["CRITICAL_TARGETS", "_build_degraded_content"],
    "BUG-071": ["_pending_task_targets", "task_id"],
    "BUG-073": ["s.value(", "or []"],  # QSettings None 兜底
}
```

**前一个里程碑**:`1dc25e7` — v2.08 Housekeeper P2 三扩展点(Task 3,由另一会话先推)。

---

### v2.08 详情(2026-05-21,Task 3 第一发)

**远端 HEAD**:`1dc25e7` — **v2.08 ✨ Housekeeper P2 三扩展点(零侵入)**。

实现 v1.90 P1 管家章节末尾留的 3 个 P2 扩展点(零侵入,只动 `housekeeper.py` + 新增测试)。

**改了什么**:

1. **`record_canon_locked_mismatch(field, expected, actual)`** — Canon locked 字段一致性巡检
   - 调用方在 `_accept_chapter_and_continue` 里循环已锁定字段,对比章节正文实际值,不一致就 record
   - 数据进 `current_report.locked_mismatches`(每项 dict:field/expected/actual,自动截断)
   - 健康度每条扣 0.1,封顶 0.3
   - oneliner 末尾显示 🔒 锁定字段被改:主角姓名/男主姓名 (+N)

2. **`check_pacing_window(n=5)`** — 跨章节奏雷达
   - 扫最近 N 章 hook_set / cool_points_count
   - 双 flat(连续 N 章无钩子且无爽点)→ 检测到节奏疲软
   - 自动 hk.warn() 一条 "节奏疲软:连续 5 章无章末钩子 + 连续 5 章无爽点"
   - 返回 dict 含 flat_hooks/flat_cools/msg/window,主程序可选择展示弹窗

3. **`snapshot_for_recovery(project_root, ch_num, keep_last=10)`** — 自动备份快照
   - 章末打项目目录 zip → `project_root/.backups/snapshot_ch{NNN}_{YYYYMMDD_HHMMSS}.zip`
   - 排除 `.backups/` 自身防套娃
   - 自动清理:保留最近 10 份,旧的按 mtime 删除
   - 失败全 return None,主程序不崩

**设计原则**(同 v1.90 P1):
   - 零侵入:只动 housekeeper.py,主程序不需要改(调用方决定接不接)
   - 失败容错:try/except 全包,主流程零干扰
   - 单测友好:全部不依赖 PyQt5,纯数据 + 文件系统

**改动汇总**:
   - `housekeeper.py`:401 → 571 行(+170),3 个新方法 + locked_mismatches 字段 + oneliner 🔒 标 + 健康度 lock_penalty
   - 新增 `test_housekeeper_p2.py`(23 测试:7 locked + 8 pacing + 7 snapshot + 1 集成)
   - `novel_ai.py`:APP_VERSION v2.06 → **v2.08**(跳过 v2.07.x 因为那些是 docs-only)

**测试零退化**:**214 passed**(149 BUG-065~074 + 19 老 housekeeper + 23 新 P2 + 4 site_preferences + 其他 19 加 1)。

**主程序集成留待用户决定**:这 3 个 P2 方法已实现 + 测试覆盖,但**主程序 `_accept_chapter_and_continue` 没主动调用**。调用方法:
   - `hk.record_canon_locked_mismatch(...)` 应在章节正文 ready 后 + canon 字段对比时调
   - `hk.check_pacing_window()` 应在 finalize_chapter 后调,显示弹窗或写日志
   - `hk.snapshot_for_recovery(...)` 应在 finalize_chapter 成功后 + 用户开了"自动备份"时调

**前一个里程碑**:`858ed1a` — v2.07.2 项目对接记忆.md 7174 → 1137 行(瘦 84%,Task 2)。
**再前**:`6db97d8` — v2.07.1 5 份过期 docs 归档到 docs/archive/(Task 1)。

---

### v2.07.2 详情(2026-05-22, Task 2)

**最新里程碑**:`(待推)` — **v2.07.2 🧹 记忆整理(Task 2)**。

`项目对接记忆.md` 从 **7174 行瘦到 1137 行(瘦 84%)**。把老 BUG 修复历史(BUG-001~064 / v1.74~v1.89,约 6050 行)用 git mv 归档到 `docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md`,保留 git 历史。

**主记忆保留**(给下个 Claude 接班的最小必要集):
  - 头部:用户信息 / 项目目标 / 项目当前状态(含 v2.06+ 里程碑链)/ 关键设计决策 / 用户偏好
  - ⚠️ 给下个 Claude 的具体警告(铁律级:版本号 / GitHub Web UI 截断 / CharacterLibrary 速查 / PAT 撤销验证陷阱 / 等 11 条)
  - 一百零六 v1.90 P1 管家(P2/P3 扩展点跟未来 Task 3 新功能直接相关)
  - 一百零七~一百一十一(v1.91~v1.95,BUG-065~069,最近活跃)
  - 末尾更新协议 + 文件签名

**归档去处**:
  - `docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md`(6102 行)— BUG-001~064 全部 v1.x 历史,顶部有"归档章节清单"导航表
  - 主记忆末尾加 "📚 归档索引" 段指向这里

**为什么不全归档,要留 v1.90/v1.91~v1.95**:
  - v1.90 P1 管家 里写了 P2/P3 扩展点(跨章节奏雷达 / 自动备份快照 / locked 字段巡检),Task 3 新功能可能用到
  - v1.91~v1.95 是最近的 BUG-065~069,Claude 上一轮还在跟,留主记忆便于接班

**改动汇总**:
  - 修改 `项目对接记忆.md`(7174 → 1137 行)
  - 新增 `docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md`(6102 行,git mv 等效效果)
  - 末尾"最近更新"+"文件签名"同步到 v2.07.2 当前状态(之前停在 v1.97 / v2.02)

**前一个里程碑**:`6db97d8` — v2.07.1 5 份过期 docs 归档到 docs/archive/(Task 1)。

---

### v2.07.1 详情(2026-05-22, Task 1)

**最新里程碑**:`(待推)` — **v2.07.1 📦 过期 docs 归档到 docs/archive/**(Task 1)。

把 5 份 v7 时代过期 docs 用 `git mv` 归档到 `docs/archive/`(保留 git 历史),加 `docs/archive/README.md` 说明每份归档原因。更新主 README 第 7 节文档导航,反映新位置。

**归档的 5 份**(共 990 行):
  - `HANDOFF_AI_WRITER_V7.md`(124 行)— v7 整合版交付清单
  - `INTEGRATION_GUIDE_V2.md`(277 行)— 寿元/伏笔/研究报告接入指南
  - `PANGU_INTEGRATION.md`(271 行)— 盘古首次接入(讲 5431 行旧主程序)
  - `README_PANGU_ADDENDUM.md`(103 行)— 盘古使用补充(被 pangu_full_spec.md 取代)
  - `WORKFLOW_PANEL_INTEGRATION.md`(215 行)— 工作流面板首次集成

**根目录活跃 docs 收敛到 3 份**:`README.md` / `项目对接记忆.md` / `pangu_full_spec.md`。

**测试零退化**:149 passed(BUG-065~074 套件)。

**前一个里程碑**:`65967d9` — v2.07 全系统集成测试 + 16 测试静态扫描适配 + README 重写。
**再前**:`c5bb8c6` — v2.06 BUG-073 + BUG-074 双 bug 修复。

---

### v2.07 详情(2026-05-22)

**背景**:用户问"继续做 然后 继续测试 所有分离的"。这一轮做了 3 件事:

**(1) 删除过期测试 test_v6.py(665 行)**:v6 时代的整体性测试,`expected_tabs` 写死了 8 个 Tab 含"小说封面生成"(已删),完全过期不可救。直接删。

**(2) 修复 test_full_integration.py 3 个过期断言**:v7 时代测试,救活后是个有价值的集成测试(48 测试覆盖窗口标题/Tab 列表/AI 配置/对话槽/lifespan/workflow/save-load round-trip 等)。改 3 处:
  - 窗口标题断言:"AI 写作工作台" → 改成 `startswith("盘古超级写作助手")`(v1.94 改名)
  - Tab 列表:从 11 个(含"小说封面生成")改成 12 个(加 🏠 项目主页 / 📚 拆书学习,去小说封面生成)

**(3) 全系统集成测试 + 修 16 个测试静态扫描挂(P3~P6 拆分遗留)**:跑全部 49 个测试文件,发现 16 个挂——全是同模式:**测试用 `open("novel_ai.py").read()` 找 PROMPTS / SITE_PREFERENCES / CharacterLibrary / BrowserWorker 等内容,但 P2~P6 已经把这些迁到 core/ ui/ ui/tabs/ 子模块,扫描扫不到**。

  **统一修法**:写一个共享 helper `tests_helpers.py::read_all_sources()`,把 novel_ai.py + 所有 ui/core 子模块 concat 起来,供静态扫描使用。所有 fail 测试改成 `from tests_helpers import read_all_sources` + 把 `open("novel_ai.py")` 换成 `read_all_sources()`。共改 16 个测试文件:`test_book_splitter_ui` / `test_canon_extract_visibility` / `test_chapter_reflow` / `test_cross_graph` / `test_cross_ref` / `test_foreshadow_auto_close` / `test_hero_state_snapshot` / `test_info_isolation` / `test_plot_progress` / `test_plot_tree` / `test_pov_mode` / `test_power_levels_extract` / `test_project_home` / `test_promise_auto_close` / `test_relation_graph` / `test_score_calibration`。`test_site_preferences` 因为 SITE_PREFERENCES 现在只在 generation_control.py(P4 迁的),用 `GEN_CTRL_PATH` 更精确。

  **结果**:**49/49 测试文件全过,822 个测试 100% pass**(比之前认知的"424 zero regression"多 398 个——大量被静默 fail 漏掉的现在救回)。

**(4) README 完整重写**:原 README 247 行,完全过期(讲"QtWebEngineView"已删 / "小说封面生成 Tab"已删 / "单文件 novel_ai.py"已拆 19 模块 / AI_URLS 缺 ChatGPT)。新 README 292 行,8 个章节,对齐 v2.06 真实状态:
  - 当前版本 v2.11 / Python 3.10+ / PyQt5 + Selenium 4.6+
  - 项目结构完整目录树(主程序 9653 行 + core/ 5 文件 + ui/ 6 文件 + ui/tabs/ 9 文件 + 17 个根级模块)
  - 累计 P1~P6 模块化进展(20740→9653,瘦 53.5%)
  - 7 个 AI 网站完整 AI_URLS
  - 章末流水线 10 个步骤表
  - 二次开发"找代码位置"速查表 + 测试命令
  - 8 份文档导航(`项目对接记忆.md` 标为"给下一代 AI 接班用")

**改动汇总**:
  - 删除:`test_v6.py`(665 行)
  - 新增:`tests_helpers.py`(48 行)
  - 修改:`test_full_integration.py`(3 处过期断言)+ `test_site_preferences.py`(读 GEN_CTRL_PATH)+ 16 个测试用 `read_all_sources()`
  - 重写:`README.md`(247 → 292 行,完全重写)
  - APP_VERSION 不变(v2.06,纯测试/文档清理不动主版本)

**测试零退化**:**822 passed / 49 文件**(从原 424 救回 398 个 + 修 16 个 fail)。

**关键决策(给下一代 Claude)**:
  - **不动 APP_VERSION**:Phase 2/3 是纯测试 + 文档清理,不涉及功能/source 改动,按"小改动末位+1"规则连版本号都不用升。如果未来添加新功能再升。
  - **`tests_helpers.read_all_sources()` 现在是测试基础设施**:以后任何 P7+ 拆分或新测试都用它,避免重蹈"扫主程序找迁走符号"覆辙。
  - **`test_full_integration.py` 是黄金集成测试**:48 个测试覆盖 Tab 顺序 + 信号 + serialize roundtrip,以后改 Tab 数量/顺序会被立刻发现,**值得保留并维护**(不能再让它过期到挂掉)。
  - **过期文档暂时保留**:`HANDOFF_AI_WRITER_V7.md` / `INTEGRATION_GUIDE_V2.md` / `PANGU_INTEGRATION.md` / `WORKFLOW_PANEL_INTEGRATION.md` / `README_PANGU_ADDENDUM.md` 都过期但留作考古,README 第 7 节标了"已过期,留作考古",用户决定要不要删。

**前一个里程碑**:`c5bb8c6` — v2.06 BUG-073 + BUG-074 双 bug 修复(QSettings None 兼容 + P3 拆分漏 PyQt5 import)。
**再前**:`818d814` — v2.05 P6 ui/browser_worker.py(BrowserWorker 2475 行外迁,主程序破万行)。
**再再前**:`c03737f` — v2.04 P5 ui/tabs/character_library.py(3860 行外迁,瘦 3857 行)。

**前一个里程碑**:`c03737f` — v2.04 P5 ui/tabs/character_library.py(CharacterLibrary 3860 行外迁,瘦 3857 行)。
**再前**:`87eaf1d` — v2.03 P4 ui/tabs/ 子包 8 Tab + core/default_skills.py + _profile_for_url 迁移(瘦 3320 行)。
**再再前**:`2d09a33` — v2.02 P3 ui/ 子包 + core/site_profiles.py(5 类 + DOM 档案外迁,瘦 720 行)。
**再再再前**:`cd4cc3f` — v2.01 P2 PROMPTS 字典外迁(瘦 583 行)。
**再再再再前**:`fd1b0eb` — v2.00 P1 core/ 子包(constants + stylesheet,瘦 145 行)。
**再再再再再前**:`8142921` — v1.98 记忆校准修订(纯文档)。

**前几个里程碑**:
- `(P3 一并归档)` — v1.97 🎯 BUG-071 `_pending_task_target` 改字典治本(A→B→C→D 路线 B-2 治本,**B 完整收官**)。**起因**:v1.96 治标修了"批量已结束"误报,但留了治本(根因 race 还在 — Canon 抽取的内容仍可能被错误地写到 `ch["summary"]`)。**根因复盘**:`_pending_task_target` 是单变量,`_send_to_ai` 时覆盖式赋值;Worker 串行 FIFO 队列,但 main UI 端连续提交两个 _send_to_ai → 单变量只保留最后一个的 meta → 第一个响应回来时拿到错误的 meta → 路由错乱。**治本方案**:`_pending_task_target` 单变量 → `_pending_task_targets` 字典,key=label(== worker 的 task_id),value=meta dict。**改动覆盖 14 处真实读写点**:`__init__` 1 处(初始化字典 + 保留旧字段做兼容)+ `_send_to_ai` 主写 1 处 + `_on_response_received` 12 处(pangu/laodao 6 路由 + 主 dispatch read/pop + 异常兜底)+ 死磕重写 1 处 + 手动抓取 1 处 + dead code `_audit_resume` 2 处(rhythm/character — 即使 dead 也改字典保持一致,加 dead code 注释明示)。**关键设计**:旧字段 `self._pending_task_target = None` 保留做兼容兜底(任何外部代码若残留访问,拿到 None 不崩);新字典 + task_id 查找 → 完全隔离。**21 BUG-071 测试**:6 静态断言(单变量真实读写归 0 + 字典使用点 + APP_VERSION + dead code 路径)+ 5 字典语义测试(并发不串台 + 三任务乱序回归 + 未知 task_id 返回 {} + pop idempotent + 死磕同 label 覆盖)+ 4 BUG-070 兼容性(防御代码完整保留 + 三档守卫 elif 链一字未改 — v1.97 后理论上不会再触发,但保留诊断)+ 4 路线纪律(v1.92/v1.93/v1.94/v1.95 既有功能未动)+ 2 init 兼容。**合跑 131 条全过**(21 BUG-071 + 9 BUG-070 + 19 BUG-069 + 7 BUG-068 + 24 BUG-067 + 15 BUG-066 + 17 BUG-065 + 19 housekeeper)。本次仅触动 `novel_ai.py`(20 处使用 `_pending_task_targets`)+ 新增 1 测试。**给下一代 Claude 的诚实告示**:race 真死透;下次实战如果摘要任务再不出现"内容混入 Canon 抽取的 1174 字符"那种串台症状 + `⚠ chain_to_next=True 但 _batch_remaining=0` 这条 BUG-070 防御 warn 不再触发 → B-2 治本生效。**严重失误反省(给下一代 Claude 的警告)**:上一轮 Claude 做完 v1.97 改动 + 本地 commit `64ef755` 后,**在 push 前**就执行了 `rm -rf push_work` 清场 — 本地 commit 永久丢失,远端只有 v1.96。**铁律补充**:**清场必须严格在远端二次确认 commit hash 之后**,不能在 commit-after-push 的同一批清理动作里混做。本轮重做了一遍,但下次如果再发生同类错误,人和 PAT 流程一样,**犯一次没事,反复犯就是流程漏洞**。**A→B→C→D 进度**:A ✅ v1.95 字数三档 → B 治标 ✅ v1.96 批量已结束防御 → **B-2 治本 ✅ v1.97 字典改造** → C(区间审查 + 逐级清除)→ D(让 AI 用 last_ch)。
**前几个里程碑**:
- `df78a9b` — v1.96 🛡 BUG-070 单章模式"批量已结束"误报防御 + 串台诊断(B 治标)。`chapter_summary` handler 末尾 elif 链改三档守卫;只动 novel_ai.py(2 处)+ 新增 1 测试 9 条全过。但留了治本(race 根因仍在),v1.97 B-2 真正解决。
- `1322c85` — v1.95 BUG-069 字数判定三档(`word_count_long` 字段不改 `word_count_ok` 语义,既有 19 测试一字未改全过)。`actual > 1.5x target` → ⚠ 不扣健康度。实战 3088/2300=1.34x 从 ✗ 回 ✓。
- `ed7e3c9` — v1.94 BUG-068 下一章选项按钮对比度修复。setStyleSheet 漏 color → 米色背景上文字不可见。教训:setStyleSheet 漏字段 = 用默认值;UI bug 必须靠截图暴露。
- `ce23313` — v1.93 BUG-067 角色 last_ch 字段 + 同名不同姓检查。tbl_chars 列 8→9 + `_find_duplicate_names` staticmethod 纯函数。关键漏改修复 L6158 load 路径列数沉默 bug。
- `9b937a8` — v1.92 BUG-066 章节锁定机制 MVP(借鉴竞品中稿/终稿,跟 v1.91 同源焦虑)。章节 dict 加 locked 字段 + 4 个改动入口拦截 + 右键菜单锁/解锁 + 🔒 前缀显示。命名避让原有 toggle_lock。
- `10d5839` — v1.91 BUG-065 关键后处理任务发送失败 → 重试 + 本地降级(摘要丢失止血)。用户点醒"检测发送键状态"切换"事后症状判定 → 按钮态判定"。
- `c16db8b` — v1.90 推送后遗漏的"最新里程碑"标签同步(纯 docs)
- `a7d2b61` — v1.90 📋 管家 P1(章末元监督上线)
- `a36bbeb` — v1.89 BUG-028 回归修复(指纹防串安装位置错位)
**前几个里程碑**:
- `cf2cfbf` — v1.88 BUG-062 workflow_pipeline 路径漏防御(对齐旧路径)
**前几个里程碑**:
- `2481103` — v1.87 跨表关联可视化("做没有的"系列收官 4/4 🎉)
- `68e3acf` — v1.86 多视角反查("做没有的"系列后续延伸 3/4)
- `392676b` — v1.85 写作模式回流(v1.80 注入的镜像)
- `0cd3a9f` — v1.84 角色 POV 模式("做没有的"系列后续延伸 1/4)
- `64563f0` — v1.83 UI 文案补扫

**`novel_ai.py`**:**20732 行 / 1.0 MB**(monolithic 巨型主文件,已远超 v7 时代的 ~8000 行规模;拆分债务持续累积)
**`pangu_system.py`**:1374 行 / 62 KB(含 v1.98 BUG-072 后 123 个禁用词 + 4 模式 + 30 项质检 + 螺旋诊断)
**测试覆盖**:**49 个测试文件**(含 BUG-065 ~ BUG-072 共 8 个 bug 专项 + pangu / lifespan / research / housekeeper / charlib / plot_tree 等模块测试)。最近一次完整精准回归(v1.98 BUG-072 收官)= **198/198 pass**(BUG-065~072 + pangu_system + pangu_patch)。早期里程碑里的 78+68+123+9+7+25=310+ 是 v7 时代的盘古集成阶段统计,**已严重过期**,真实总用例数(`grep -hE 'def test_' test_*.py | wc -l`)约 **900+**,但其中含若干被跳过/废弃用例(如 `test_quick_bar` 已 deprecated)。

### 文件结构(2026-05-21 v1.98 已校准)

```
novel_ai/
│
│  # ── 主程序 ─────────────────────────────────────
├── novel_ai.py                       # 主程序 monolithic, 20732 行 / 1.0 MB
│
│  # ── 模块化扩展(都走 try/except + X_AVAILABLE flag 接入)────────
├── workflow_pipeline.py              # 模块化生成流水线
├── workflow_panel.py                 # 工作流可视化面板(Tab)
├── lifespan_loops_steps.py           # 寿元台账 + 长期伏笔检查
├── lifespan_loops_panel.py           # 上面对应的 Tab
├── research_report_skills.py         # 研究报告出厂技能
├── housekeeper.py                    # 章末管家(质量/字数/钩子/伏笔 监督)
├── dialogue_critic.py                # 对话质量评判 + 自动修复
├── flow_rl.py                        # 行为/奖励埋点(RL-lite,选择当前发送策略)
├── relation_graph.py                 # 关系网力导向图后端(配 vis-network 前端)
├── project_io.py                     # 项目 JSON 存档读写
├── tts_backend.py                    # TTS 朗读后端(edge-tts / index-tts / pygame)
├── book_splitter.py                  # 长稿章节切分
├── import_continuation.py            # 续写导入
├── license_guard.py                  # 许可检查 + 网络验证
├── patch_novel_ai.py                 # 老版本兼容补丁(历史包袱,慎动)
│
│  # ── 盘古引擎 ⭐ ─────────────────────────────────
├── pangu_system.py        ⭐         # 盘古引擎(1374 行 / 62 KB / 123 禁用词 / 4 模式 / 30 质检)
├── pangu_patch.py         ⭐         # 零侵入安装器,wrap PROMPTS
├── pangu_full_spec.md     ⭐         # 48K 字盘古 V1.0 完整规范(源文档)
├── PANGU_INTEGRATION.md   ⭐         # 集成手册
├── README_PANGU_ADDENDUM.md ⭐       # README 待合并章节
│
│  # ── 测试 49 个 .py ──────────────────────────────
│  # BUG 专项:test_bug065 ~ test_bug072(8 个)
│  # 模块测试:test_pangu_system / test_pangu_patch / test_housekeeper
│  #            test_lifespan_loops(_panel) / test_research_report_skills
│  #            test_dialogue_critic(_autofix) / test_book_splitter(_ui)
│  #            test_relation_graph / test_cross_graph / test_cross_ref
│  #            test_charlib_import / test_plot_progress / test_plot_tree
│  #            test_pov_mode / test_hero_state_(sync|snapshot)
│  #            test_foreshadow_auto_close / test_promise_auto_close
│  #            test_info_isolation / test_canon_extract_visibility
│  #            test_power_levels_extract / test_chapter_reflow
│  #            test_hook_detection / test_prev_context_multi
│  #            test_score_calibration / test_regression_consistency
│  #            test_site_preferences / test_tts_backend
│  #            test_import_continuation / test_project_io / test_project_home
│  #            test_mainwindow_signals / test_reset_ui_state
│  #            test_e2e_novel_ai_integration / test_v6 / test_full_integration
│  #            test_workflow_panel / test_quick_bar(deprecated)
├── test_fixtures/                    # 测试夹具数据
├── vendor/                           # 第三方资源(vis-network 等)
│
│  # ── 文档 ────────────────────────────────────────
├── README.md                         # ⚠ 已严重过期(还写 Playwright + 单文件)
├── INTEGRATION_GUIDE_V2.md
├── HANDOFF_AI_WRITER_V7.md
├── WORKFLOW_PANEL_INTEGRATION.md
├── 对话记忆_v1.98_BUG-072.md         # 单次会话工作台(短期记忆)
└── 项目对接记忆.md                    # 本文件(长期账本)
```

**⚠ 给下一代 Claude 的现状速查**:**不要被 GitHub 网页文件列表骗了** —— GitHub Web UI 在 file count > 20 时会**截断显示**只列首 20 个,把 `pangu_*` / `housekeeper` / `dialogue_critic` / `flow_rl` / `relation_graph` / 8 个 `test_bug0XX` 等关键文件**全藏起来**。任何需要看仓库全貌,**必须 clone** 或用 GitHub API,不要看 Web 文件树。

### PROMPTS 字典(novel_ai.py 第 140–723 行,**29 个键**)

| 状态 | 键 | 说明 |
|---|---|---|
| ⭐ 被盘古包裹 | `chapter` | 头加铁律(123 禁用词 + 视/听/触/情绪/句式/感官/智商/AI文风防火墙),尾加输出格式 |
| ⭐ | `golden_three` | 头加铁律,尾加【黄金三章公式】(绝境羞辱→金手指激活→首次反转打脸)+ 循环爽点 |
| ⭐ | `outline_full` / `outline_part` | 头加铁律,尾加【矛盾螺旋大纲规范】(主要矛盾 + 人物弧光三阶段 + P1-P7 标注) |
| ⭐ | `ai_optimize` | 替换为【雕刻家模式】:先删再改、能砍的不改 |
| ⭐ | `creative_inspiration` | 加轻量约束:反光/影子/另一个自己题材禁用 + 情绪直给 |
| 不动 | `title` / `intro` | 取书名 / 写简介,工具型 prompt 不污染 |
| 不动 | `chapter_summary` / `character_extract` / `long_term_extract` | 对话记忆维护 |
| 不动 | `canon_audit` / `canon_extract` | 防崩审稿(B/C/D 自鞭策机制) |
| 不动 | `critique_rhythm` / `critique_character` | 节奏 / 人设审稿 |
| 不动 | `style_audit` / `world_extract` | v7 后期新增,盘古不识别也不破坏 |
| 不动(v1.76+) | `foreshadow_check` / `foreshadow_reeval` | 全自动伏笔闭环(BUG-056) |
| 不动(v1.77+) | `promise_check` / `promise_reeval` | 威胁承诺自动闭环(BUG-057) |
| 不动(v1.78+) | `arc_advance_check` | 剧情进度管理(BUG-058) |
| 不动(v1.79+) | `info_check` / `info_disclose_check` | 信息隔离控制(BUG-059) |
| 不动(v1.79+) | `relation_change_check` | 关系值变化抽取 |
| 不动(v1.80+) | `chapter_to_plot_node` | 剧情树规划(BUG-060) |
| 不动(v1.81+) | `critique_laodao` / `laodao_autofix` | 老刀审稿门 + 自动修复 |
| 不动(v1.81+) | `pangu_autofix` | 盘古死磕自动修复(评分门) |

⚠ **PROMPTS keys 从 v7 时代 17 个 → v1.98 时代 29 个**。新增的 12 个 key 大多是"做没有的"系列(v1.76-v1.80)和评分/修复管道(v1.81+)。**未来加 key 前先看 `pangu_patch.py` 的 `DEFAULT_SCENARIO_MAP`** —— 章节型 prompt 想被盘古包裹必须在那里加映射;**非章节型(extract / check / autofix)严禁让盘古碰**,会污染评判 JSON。

### 集成激活代码(novel_ai.py 第 725–729 行)

```python
# ---- 盘古超级系统(零侵入集成,新增) ----
try:
    from pangu_patch import install_pangu
    install_pangu(globals())  # 就地把 PROMPTS 字典套上盘古铁律
    PANGU_AVAILABLE = True
except ImportError:
    PANGU_AVAILABLE = False
```

import 时 PROMPTS 就地被包裹:`chapter` prompt 从约 400 字 → 2192 字。
所有 `{title}` / `{chapter_num}` / `{genre}` / `{outline}` / `{chapter_outline}` / `{target_words}` / `{min_words}` 占位符**完整保留**,`.format()` 调用零修改。

### GUI 接入

**CreationSettings(创作设置 Tab)**:
- AI 配置组下:盘古超级系统**总开关 checkbox**(默认 ✓,可即时切换 install/uninstall)
- 创意灵感行下:**🛕 盘古快捷工具组**
  - 🎯 风格匹配按钮(基于关键词推 Top 3 主辅风格 + 平台)
  - 🏗️ 建筑师 / 🎭 造梦师 / ⚗️ 炼金术士 / 🗿 雕刻家 四模式快捷切换

**ChapterEditor(章节编辑器 Tab)**:
- 工具栏:3 个新按钮
  - 🛡️ 本地词扫(0-token,纯字符串匹配毫秒级)
  - 📊 30 项质检(调 AI 跑盘古 30 项深度审稿,返回 JSON)
  - 🌀 螺旋诊断(调 AI 诊断章节处于 P1-P7 哪个阶段)

**保存章节钩子**:
- `save_current_chapter()` 开头自动跑 `quick_chapter_lint()`,未通过只在日志警告,不阻断保存

**Phase A+B(2026-05-15 第二批)**:
- 章节编辑器红波浪线**实时高亮**禁用词(`_PanguForbiddenHighlighter`,QSyntaxHighlighter 子类)
- 章节编辑器加 👁️ **预览 Prompt** 按钮(查看 2200+ 字盘古加持后的完整 prompt)
- 创作设置 Tab 加**白名单输入框**(空格/换行分隔,持久化到 QSettings,避免误杀)
- 顶部工具栏 ❓ **盘古手册**(QTextBrowser 渲染 48K spec,带搜索 / 跳转)
- 顶部工具栏 🛡️ **全书巡检**(扫描所有章节,出报告:平均分 / 通过率 / TOP10 禁用词 / 各章详情,可导出 .md / .html)
- 30 项质检结果**自动段落标注**(_on_response_received 解析 JSON,把 advice 关键词映射到段落,黄底高亮)
- 首次启动**自动弹欢迎 banner**(QSettings flag 控制只弹一次)

---

## 🔧 关键设计决策(不要打破)

### 1. 零侵入扩展模式
所有新模块都用 `try: from X import Y; X_AVAILABLE = True; except ImportError: X_AVAILABLE = False` 接入:
- `workflow_pipeline` / `WORKFLOW_AVAILABLE`
- `workflow_panel` / `WORKFLOW_PANEL_AVAILABLE`
- `lifespan_loops_steps` / `LIFESPAN_LOOPS_AVAILABLE`
- `research_report_skills` / `RESEARCH_SKILLS_AVAILABLE`
- ⭐ `pangu_patch` / `PANGU_AVAILABLE`

### 2. 盘古集成位置不能动
`install_pangu(globals())` 必须放在 `PROMPTS = { ... }` 字典**之后**(第 333 行 `}` 后)。
**改顺序会失效但不会报错** —— 静悄悄不生效是最坑的 bug。

### 3. GUI 入口也走 try/except 防护
所有 GUI handler(`_on_pangu_*`)开头都 `try: from pangu_system import ... except ImportError: 弹错误框 return`。
即使用户删了 `pangu_*.py` 也不会让 novel_ai.py 启动崩溃。

### 4. 一键回滚的方式
- **方法 1**:GUI 取消勾选盘古 checkbox → `uninstall_pangu(globals())` → 恢复原 PROMPTS
- **方法 2**:`install_pangu(globals(), enabled=False)` → 加载但不 patch
- **方法 3**:注释 `novel_ai.py` 第 725–729 行 try 块
- **方法 4**:删 `pangu_*.py` 文件 → try 自动走 ImportError 分支

### 5. 盘古不重复定义已有审稿系统
仓库已有的 `canon_*` / `critique_*` 是 B/C/D 自鞭策审稿管线,跟盘古铁律功能不重叠 —— 盘古管"写之前的约束",审稿管"写完之后的检查"。**不要让盘古碰这些键**。

### 6. PROMPTS 占位符完整性
盘古 wrap 时把铁律 / 公式 / 输出格式加在 prompt **头尾**,**绝不动 `{placeholder}`**。所有 `.format(...)` 调用零修改。

### 7. 用户后续可能加新 PROMPTS 键
v7213 → v7222 之间用户已经加了 `style_audit` / `world_extract`。盘古的 patch 只识别 `DEFAULT_SCENARIO_MAP` 里的 6 个键,不识别的保持原样。**未来如果加章节型 prompt 想被盘古包裹**,在 `pangu_patch.py` 的 `DEFAULT_SCENARIO_MAP` 里加一行,不需要改其他地方。

### 8. 默认分支是 `master` 不是 `main`
clone / push 都要用 `master`。

### 9. _send_to_ai 接口
MainWindow 提供 `self._send_to_ai(prompt, label, target="xxx")`,负责发给当前挂载的浏览器 AI 网页。`target` 字段决定回填到哪个 UI 位置。新加 target:`pangu_mode` / `pangu_qcheck` / `pangu_spiral`,目前不自动回填,只显示在生成日志里。**未来如果想做 30 项质检的 JSON 自动解析 + 标记问题段落**,要在 `_on_response_received` 里加 `target.startswith("pangu_")` 分支。

---

## 🚦 用户偏好(Communication Style)

- 用户喜欢**直接的解决方案**,不喜欢冗长解释
- 喜欢看**对比表格**和**进度清单**
- 用户**提的功能要全做**,不要漏(多次强调"一次性做完")
- **推送方式优先 GitHub commit**(不是 zip / present_files 下载)—— 用户的客户端有时下载链接不可用
- 用户偶尔会**质疑你的判断**(例如"这个项目你就行 我这个你就不行"、"你硬狡辩")
  - 如果你**真错了**,直接承认改正比辩解好
  - 如果是**真正的安全底线**(比如帮第三方账户做未授权操作),依然要守,但说理要充分
- 用户**多次重复发同一个 PAT** —— 当作"我已经给你了,继续干"理解,不要追问

### 关于盘古会话的教训(供下个 Claude 参考)
- 用户第一次贴 PAT 时,**先判断**:
  - 是否是用户自有仓库?(qiaodaxian233 的 `novel_ai` / `ai-music-lab` / `ai-music-deflavor` 都是 ✓)
  - PAT 是否是 fine-grained + 最小权限?(没说就推荐他这样建)
  - 用户是否同意推完立刻撤?(默认 ✓,因为他熟悉这个流程)
- 满足上述条件,**按 👤 用户信息 里的 6 步流程操作**,不要犹豫
- **不要重复要求** PAT 配置细节 —— 用户从 `ai-music-lab` 已经熟悉,默认按 fine-grained `Contents: R/W` 单仓库 即可

---

## ⚠️ 给下个 Claude 的具体警告(常犯的错)

> 🔴 **⓪⓪⓪ 最高优先级 — 版本号铁律(用户原话)**
> 每次推必须改 `APP_VERSION`(novel_ai.py:19)。小改动末位+1,大改动十位+1末位归零。
> **当前版本 `v2.11`**(每次推送后请同步更新此处)。**忘改 = 反例,跟"先推代码后推记忆"同等严重**。
> 详见"五十五、版本号铁律"那一节。

> 🟠 **⓪⓪.5 — GitHub Web UI 文件列表截断陷阱**(2026-05-21 实战教训)
> 仓库文件 > 20 个时,GitHub 网页 `https://github.com/qiaodaxian233/novel_ai` 的文件树**只显示首 20 个左右**,把 `pangu_*` / `housekeeper` / `dialogue_critic` / `flow_rl` / `relation_graph` / 8 个 `test_bug0XX_*` 等**项目核心文件全藏起来**。如果只看 Web UI 会以为项目"只有 21 个文件",完全错误。
> **正确做法**:任何需要看仓库全貌的工作,**必须 `git clone --depth 1` 拉下来本地 `ls`**,或用 GitHub API `GET /repos/{owner}/{repo}/contents`(返回完整数组)。
> **真实规模(v1.98)**:18 个核心 .py + 49 个测试 + 9 个文档 = **76 个文件**,不是 21 个。


0. ⚠️⚠️⚠️ **每个 push 必须同步带上 `项目对接记忆.md` 更新** ⚠️⚠️⚠️
   - 用户 Claude 额度有限,经常被"对话额度不足"卡断 → 新会话开始
   - 你(下个 Claude)能不能续上,**完全靠**这个文件
   - 所以**绝对不要**先推代码 commit 再说"下次推记忆"。"下次"很可能就是新会话,你看不到旧 Claude 心里那本账
   - 每次 PAT 来了准备推之前,**先**:
     - (a) 在记忆里加这次的里程碑 / BUG 修复 / 决策 / 待办进展
     - (b) 然后 `git add 项目对接记忆.md` 和代码改动**一起 commit 一起 push**(或者两个分开 commit 但同一次 push)
   - 唯一例外:**纯文档** 单独 push 可以(像这条规则本身的更新)

0.5 ⚠️ **`CharacterLibrary` 已存在(Tab 名 🎭 角色与世界 / `self.tab_charlib`)** ⚠️
   - **13 个子表/子页**(v1.81 时):角色(`tbl_chars`)/ 关系(`tbl_relations`)/ 关系网(v1.70)/ 时间线(`tbl_timeline`)/ 物品(`tbl_items`)/ 战力(`tbl_power`)/ 伏笔(`tbl_fore`)/ 威胁承诺(`tbl_promises`,v1.77)/ 剧情进度(`tbl_arcs` + `tbl_rel_values` + `tbl_goals`,v1.78 嵌 3 子表)/ 信息隔离(`tbl_infos` + `tbl_known_by`,v1.79 嵌 2 子表)/ 剧情树(`tree_plot` QTreeWidget,v1.80)/ 钩子编年 / 爽点编年
   - **完整抽取链已存在**:`PROMPTS["world_extract"]` + `_charlib_extract_from_chapters` + `_on_world_extract_received` + `_merge_into_charlib`
   - 用户说"自动同步到库" → **不要新建 Tab**!**`CharacterLibrary` 就是答案**。`chk_auto_extract` 开关 + `charlib_extract` step 接入 `_post_chapter_chain`
   - **5 阶段章末 pipeline**(v1.79 起):`foreshadow_check → promise_check → arc_advance_check → relation_change_check → info_disclose_check → info_check` — 加新阶段必须挂在最后,且加 `X_pos > Y_pos` 顺序测试守
   - 还有一个独立的 `CanonGuard` Tab(`self.tab_canon`,Tab 名 "Canon 设定"),那是文本框式的设定档,**不要跟 `CharacterLibrary` 混淆** —— 两个并存,Canon 是简化版,CharLib 才是大库
   - 第四批 Claude 犯了"以为 4 库不存在"的错。"做没有的"4 步(v1.77-v1.80)又加了 4 个新 sub-tab。这条警告就是给后人的"现状速查"

1. **写代码后必须真实运行测试一遍**,不要"看起来应该没问题就推" —— Phase A+B 那次就是这么 ship 出 4 个 BUG 的
2. **str_replace 锚点要看清楚类的边界**,否则会像 banner 那样插错类里
3. **格式化字符串调用要核对函数签名**,`build_style_report` 那次 topk 就是想当然加的
4. **可移植性优先**:不要硬编码 `/home/claude` 等开发机路径,用 `Path(__file__).resolve().parent`
5. **测试脚本不要顶层 `sys.exit`**,会阻塞 pytest/unittest discover
6. **`_parse_score` 这类解析器要先尝试新格式(JSON)再 fallback 旧格式(8/10)**,反过来会被无脑匹配数字坑
7. **`SyntaxWarning` 在 Python 3.12+ 是真问题**:JS 嵌 Python 字符串要用 `r"""`,正则同理
8. **f-string 双大括号转义只在 f-string 内部生效**:`{{x}}` 在普通字符串/普通代码行里是 `{ x }`(set of x)。BUG-011 就是这么栽的 —— 看到 `execute_cdp_cmd('Input.insertText', {{'text': text}})` 上一行是 `f"""..."""`,但 f-string 已经闭合,这一行就是普通代码,`{{...}}` 不是转义。**改 CDP 调用类代码前先检查这一行是不是真的在 f-string 内**
9. **DOM 选择器要精确到 assistant 主体**:DeepSeek 的 `div.ds-markdown` 这种泛 class 同时被思考过程块 / 用户提问块戴在身上,`querySelectorAll(...)[last]` 不一定是 AI 正式回复。**精准 anchor 类(`ds-assistant-message-main-content`)** 才能稳定抓到主体,宽匹配只能放 fallback。BUG-012 就是这么栽的
10. **用户贴 DOM/日志/报错原文是金钥匙**:把"AI 回复看不懂"具体化到 class 名和字符数,5 分钟锁定根因。**主动让用户贴原文** 比 Claude 自己瞎猜效率高 10 倍

11. ⚠️ **PAT 撤销验证的双重陷阱**(2026-05-20 实战教训,**别再栽**) ⚠️

    用户走完 PAT 推送流程后说"撤了",你需要验证 PAT 真的已死。这里有两个 Claude 极容易栽的坑:

    **坑 A — 用 `git ls-remote` 测 public repo 的 PAT 是空测试**

    `qiaodaxian233/novel_ai` 是 **public 仓库**。public repo 的 `ls-remote` / `fetch` / `clone` 在 GitHub 上**不需要任何认证**,匿名访问就能拿到 refs 和对象。所以以下代码看起来在测 PAT,实际上 PAT 是死是活返回的都是同一个 commit hash:

    ```bash
    # ★ 错!这测的是"repo 是不是 public + GitHub 在不在线",不是 PAT
    git remote add origin "https://x-access-token:${GIT_PAT}@github.com/qiaodaxian233/novel_ai.git"
    git ls-remote origin master   # 返回 hash 不等于 PAT 活;返回错也不等于 PAT 死
    ```

    **正确做法**:用**写操作**当探针,记忆文件里写的就是 `git push --dry-run`,因为写操作必须鉴权,即使在 public repo 上。

    ```bash
    # ✓ 对!写操作探针,public repo 也强制鉴权
    git clone -q "https://x-access-token:${GIT_PAT}@github.com/qiaodaxian233/novel_ai.git" repo
    cd repo
    GIT_TERMINAL_PROMPT=0 git push --dry-run origin master 2>&1
    # PAT 死:stderr 含 "Authentication failed" 或 "Invalid username or token"
    # PAT 活:stderr 含 "Everything up-to-date" 或 dry-run 报告
    ```

    **坑 B — 管道末端的 `$?` 不是 git 的退出码**

    PAT 嵌在 URL 里,日志/错误消息里会原样出现,要 sed 脱敏。但如果脱敏管道写成这样:

    ```bash
    # ★ 错!$? 接的是 sed 的退出码,不是 git push 的
    git push --dry-run origin master 2>&1 | sed "s|${GIT_PAT}|***|g"
    RC=$?     # 永远 = 0(sed 成功),不管 git 是否鉴权失败
    if [ $RC -eq 0 ]; then echo "PAT 还活"; fi   # 永远走这条 → 误判
    ```

    **正确做法**:用 `${PIPESTATUS[0]}`(bash)抓管道左端;或者先把 git 输出存文件再 sed,或者完全不脱敏 stderr 改成关键词 grep:

    ```bash
    # ✓ 方案 1:PIPESTATUS
    git push --dry-run origin master 2>&1 | sed "s|${GIT_PAT}|***|g"
    RC=${PIPESTATUS[0]}

    # ✓ 方案 2:不用 $?,直接 grep 错误关键词
    OUT=$(GIT_TERMINAL_PROMPT=0 git push --dry-run origin master 2>&1)
    echo "$OUT" | sed "s|${GIT_PAT}|***|g"
    if echo "$OUT" | grep -qE "Authentication failed|Invalid username"; then
        echo "✓ PAT 已死"
    else
        echo "★ PAT 仍然有效"
    fi
    ```

    **更高阶的教训**:当用户说"撤了"而你的测试说"还活",**第一反应应该是怀疑测试方法,而不是怀疑用户**。我那次连着三轮坚持"PAT 还活,你撤错了 token",让用户去翻 classic / fine-grained 两个列表瞎找,最后用户一句"不是我仓库你能访问就是 pat 没撤吧"才点醒——public repo 的访问跟 PAT 半毛钱关系没有。**用户从场景常识看出测试逻辑漏洞的速度比 Claude 反思自己快**。当测试结果跟用户陈述冲突时,先重审测试有没有测对东西,再去质疑用户。

---

## 一百零六、v1.90 — 📋 管家 P1(章末元监督上线)

### 起因 — 用户原话

v1.89 推完(用户和 Claude 各自独立做了同一个 BUG-028 修复,用户先 push,Claude 这边在 rebase),用户立刻说"继续 开始做管家系统"。澄清"管家":
> "他会管理硬性条件 这些流程中出错 防止写作中丢东西。会自动抽取入库
> 自动监督伏笔是否创建/回收。"

**核心要求**:不是新写一堆功能,而是把现有"零散自动化"统一到一个**元监督层**——
站在所有写作步骤之上,审计、补救、告警。"管家不替代业务,管家巡查业务有没有跑"。

### 实际审计 — 用户大部分功能已有,只是没聚合

Claude 列了 12 项管家职能,审计后:

| # | 状态 | 备注 |
|---|---|---|
| 1 硬性条件审计 | ✅ 已有(WordCountStep / dialogue_critic / pangu 38 项) |
| 2 防丢双检 | ✅ 已有(BUG-027 哨兵 / parse_chapter_meta) |
| 3 6 库自动抽取 | ✅ 已有(v1.32 BUG-021 + canon_extract) |
| 4 伏笔自动闭环 | ✅ 已有(BUG-014 / BUG-056 / lifespan_loops) |
| 5 威胁承诺追踪 | ✅ 已有(v1.77 BUG-057) |
| 6 一致性巡检 | 🟡 半有 |
| 7 节奏雷达(跨章) | ❌ 没有 |
| 8 pipeline 健康度自检 | ❌ 没有(BUG-062 暴露的盲区) |
| 9 自动备份快照 | 🟡 半有 |
| 10 RL 反馈通道 | ✅ 已有(flow_rl) |
| 11 管家日报仪表 | ❌ 没有(P1 重点) |
| 12 二道闸巡查 | ❌ 没有 |

**真正缺口只有 5 项**——P1 选 #1+#2+#8+#11 优先做,P2/P3 留 #6/#7/#9/#12。

### P1 实现 — 三个文件

**1. `housekeeper.py`(新文件,~380 行)**

核心两个类:
- `HousekeeperReport`(@dataclass)— 单章审计报告。字段:`chapter_num` /
  `path_tag`(main vs workflow)/ `pipeline_ran`(9 个步骤完成情况)/ `content_len_raw`
  / `content_len_normalized` / `pangu_meta_stripped_chars` / 钩子爽点伏笔选项
  计数 / 6 库抽取计数 / 字数门 / 13 法 reds 数 / `warnings` 列表 / `health_score`
- `Housekeeper` — 章级管家。生命周期:`start_chapter(ch_num, path_tag)` →
  各种 `record_xxx()` 收集信号 → `finalize_chapter()` 关闭并入历史

健康度算法(`HousekeeperReport._compute_health`):
```
score = 0.6 * (pipeline 完成率) + 0.4 * (硬性条件分) - 0.05 * warn_count(上限 0.3)
≥ 0.85 = 🟢 / 0.65-0.85 = 🟡 / < 0.65 = 🔴
```

oneliner 输出(章末日志一行总结):
```
🛎 第7章管家:字数✓2456 | 钩/爽×2/埋3/收1/选×2 | +chars×2 +rels×1 |
              13法✓(说/道 2/5) | pipe 9/9(100%) | 🟢 95%
```

Session summary(`render_session_summary`):
```
📋 管家 · 本次会话汇总(45.3 分钟)
  章节产出:7 章 · 平均健康度 🟢 91%
  伏笔:埋 14 收 9(闭环良好)
  6 库累计抽取:chars+8 rels+5 timeline+3 items+2
```

模块级单例 `get_housekeeper()` 给 novel_ai.py 直接 import 使用。

**2. `novel_ai.py` 挂钩(~80 行)**

主路径 `_accept_chapter_and_continue`(line ~15960):
- 入口:`hk.start_chapter(ch_num, path_tag="main")`
- pangu_meta 解析后:`hk.record_content(content, body_clean)` + `hk.record_pangu_meta(pangu_meta)`
  - 失败分支:`hk.record_pangu_meta_failed(reason)` / `hk.warn("元信息剥离失败")`
- 两个 sync 后:`hk.record_step("seeds_sync_lifespan", True)` + `..."hook_cool_sync"`
- auto_save 后:`hk.record_step("auto_save", True)`
- 字数日志前:`hk.record_word_count(target, actual)`
- 13 法扫描后:`hk.record_dialogue_critic(reds, say_count, say_allowed)`
- 末尾:`hk.record_step("post_chapter_chain", True)` + `final = hk.finalize_chapter()` +
  log `final.render_oneliner()`

工具菜单加一项:`📋 管家日报(本次会话章节健康汇总)`→ 弹窗显示 session summary +
最近 5 章的 oneliner。

**3. `workflow_pipeline.py` 挂钩(~40 行)**

`_accept`(line ~767)同样的挂载,`path_tag="workflow"` 标识来源。
未来如果观察到两路径的 oneliner 系统性不同,就是 BUG-062 那种"路径漂移"复发的早期信号。

### 设计原则(给下个 Claude)

1. **管家不写业务,管家只观测** — 所有 record_xxx 都是"看一眼,记一笔",
   完全不改变现有行为。每个 hook 包在独立 try/except 里,失败静默吞,不影响主流程。
2. **零侵入** — `HOUSEKEEPER_AVAILABLE` 标志位 + 顶部条件 import。模块丢了,
   整个程序无感降级,所有 hook 自动跳过。
3. **单测友好** — `housekeeper.py` 不依赖 PyQt5、不依赖 selenium,纯数据结构,
   `test_housekeeper.py` 19 个用例覆盖全 API。
4. **数据驱动健康度** — 不依赖任何主观判断,公式可调,字段可加。
5. **两路径对称挂载** — main 路径和 workflow 路径都挂,用 `path_tag` 区分,
   未来跑长了能看出"路径漂移"。

### 测试

- `test_housekeeper.py` 19 个用例:基础生命周期 / record_content / pangu_meta /
  字数门 / 健康度 / oneliner / session summary / 单例 — 全过
- `test_regression_consistency.py` 10/10 保持全过 — 没回退现有功能
- 总计 29 passed

### Rebase 教训(本次会话踩的坑)

用户和 Claude 同时独立做了 BUG-028 修复(等价但位置略不同:用户挪到 line 9102 之 tm_bridge 清理后 / Claude 挪到 line 9062 之 prev_count 旁),用户先 push 成 `a36bbeb`。Claude 这边 push 时 `! [rejected]` —— 远端有 Claude 本地没有的 commit。

**正确做法**:
1. `git fetch origin master` 看远端有啥
2. 对比本地 commit vs 远端 commit,判断是不是"做了同一件事"
3. 如果是,`git reset HEAD~1` 撤本地 commit + `git stash push -u` 保存改动 +
   `git pull --ff-only` 拉远端 + `git stash pop` 弹回,然后手动 resolve
   冲突区域(用远端版本作 base,只重新 apply 没冲突的新功能部分)
4. 重新 commit 时,**只保留增量(管家 P1),不再重复 v1.89**

**别走的弯路**:`git checkout --theirs` 在 stash pop context 里的"theirs"是 stash 那边(你这边),不是 HEAD 那边(远端)。语义反了。要取远端就用 `--ours`,或者直接 `git checkout HEAD -- <file>`。

### 改动统计

- `housekeeper.py` 新增 380 行
- `test_housekeeper.py` 新增 256 行
- `novel_ai.py` +~80 行(管家 hook 6 处 + 工具菜单 + show_housekeeper_summary)
- `workflow_pipeline.py` +~40 行(workflow 路径 5 处 hook)
- APP_VERSION v1.89 → v1.90
- `项目对接记忆.md` 本节 + 顶部里程碑 + 文末 + 当前版本号

### P2/P3 留的扩展点

| 阶段 | 内容 | 在 housekeeper.py 怎么加 |
|---|---|---|
| P2 | locked 字段一致性巡检(#6) | 新加 `record_canon_locked_mismatch(field, expected, actual)` |
| P2 | 跨章节奏雷达(#7) | 新加 `check_pacing_window(N)`,扫最近 N 章 hook/cool 单调 |
| P2 | 自动备份快照(#9) | 新加 `snapshot_for_recovery()`,finalize_chapter 时打 zip |
| P3 | 二道闸巡查(#12) | 新加 `verify_defenses(["BUG-027", "BUG-028"])` |
| P3 | RL 反馈联动(#10 加强) | finalize 时把 health_score 也喂给 flow_rl 当奖励维度 |

### 给下个 Claude 的核心提示

- 管家是**新基础设施**,不是新功能。后续所有"自动监督"类需求都应该加到管家里,
  不再散落在 _accept_chapter_and_continue 各处。
- **任何"章末 X 失败了我想知道"的需求,都翻译成 `hk.warn(msg)`** 即可。
  oneliner 末尾会显示。
- **管家面板(UI)是 P1 最薄弱处** — 现在只有"工具菜单 → 管家日报弹窗"。
  P2 可以做成生成 Tab 的常驻面板,每章生成完实时刷新。

## 一百零七、v1.91 — BUG-065 关键后处理任务失败 → 重试 + 本地降级(摘要丢失止血)

### 起因 — 用户实战日志

v1.90 推完用户立刻实战验证(2026-05-21,单章模式写第 4 章)。前面信号全部 ✓:章末 `🛎 第4章管家:字数✗3397 | 钩/爽×2/埋1/收1/选×3 | pipe 5/5(100%) | 🟡73%` 出现(P1 hook 落地),Canon/节奏/人设稽核字数 32/83/92 不再跟章节正文 3397/4032 撞数字(BUG-028 修复有效)。但**末尾 4 秒冒出新问题**:

```
14:27:39 ✓ 任务『Canon抽取-第4章』抓取成功,642 字符
14:27:39 ✓ 第 4 章摘要已记入对话记忆
14:27:39 ℹ 准备发送:摘要-第4章 (3834 字符)
14:27:39 ℹ textarea 注入: OK_TA
14:27:39 ℹ 批量生成已结束           ← !!! 单章模式不该有"批量已结束"
14:27:40 ℹ 已按 Enter 发送
14:27:43 ⚠ Enter 后 5s 仍未确认发送(2→2 / textarea 未清空 / 无 AI 写迹象)
14:27:43 ℹ 🤖 RL 建议跳过策略 B(避免误点 stop),return False
14:27:43 ✗ 回车与发送按钮均失败,放弃本次任务
14:27:43 ⚠ 任务『摘要-第4章』未抓到内容(选择器需调整)
```

用户原话:**"这里不能放弃啊。得改啊"** — 关键任务"放弃"等于第 4 章对后续章节不可见,数据丢失。

### 用户点醒的关键洞察

Claude 先方案了一堆"任务级重试 / 本地降级 / 根因(cleanup 竞态)三层补防",写了一堆 P0/P1/P2 措辞。用户一句话戳穿:

> "**检测发送键处于什么状态这个没用吗?**"

这一刀切中要害——Claude 之前的方案都在**事后症状**上打转(消息数、textarea 清空、AI 写迹象),**忽略了最直接的当前态信号**:发送按钮当前是什么状态(亮 / 灰 / stop / loading)。

### 现状审计

`_dispatch_send` Enter 后 5s 等待用了 3 个判定信号(都是"事后症状"):
1. 消息计数 `_after_cnt > _before_cnt`(短回复时失灵)
2. AI 正在写(stop 按钮 SVG rect 出现)
3. textarea 已清空

**缺失的**:发送按钮**当前态**枚举判定。代码里其实零散有:9919 行注入后"等按钮渲染出来"、10272 行策略 B `removeAttribute('disabled')` 强行点击、10292 行排除 stop 防误点 — 但都没聚合成"按一次问按钮当前是什么"的状态机。

诊断结果:14:27:43 那次"事后症状全阴性",但**当时按钮可能是 disabled(textarea 被 cleanup 清了)或 enabled(Enter 焦点丢)** — 这两个根因修法完全不同,日志看不出来,所以现有重试逻辑等于闭眼瞎打。

### 修复 — 四件套

**1. `_get_send_button_state()` 新增 — 单一信号源**(novel_ai.py L10191+)

返回 dict:`{state: 'enabled'|'disabled'|'stop'|'loading'|'none', detail: 文字诊断}`
- 通用 selector(`composer-submit-btn` / `[data-testid="send-button"]` / `aria-label*=发送`)+ DeepSeek 兜底(textarea 右下方 `[role=button]:has(svg)`)
- 优先级:stop(`svg rect` / aria 含"停止") > loading(spinner / animate-spin)> disabled(`btn.disabled` / `aria-disabled=true`)> enabled
- `none` 表示 DOM 里找不到按钮(可能正在重渲染),让上层"等等再问"

**2. `_dispatch_send` Enter 后判定新增"检测 4"**(novel_ai.py L10390+)

3 个事后症状都没触发时,加一道按钮态旁证:`state ∈ {stop, loading}` → 立即认定发送成功(AI 已接管,这是最强证据)。

Enter 后 5s 仍失败的告警 log 增加按钮态诊断,事后能从日志直接定位根因。

**3. `_send_prompt` 失败路径分级**(novel_ai.py L9178+)

```python
CRITICAL_TARGETS = {
    "chapter_summary", "canon_extract",
    "character_extract", "world_extract", "long_term_extract",
}
```

关键任务(target ∈ 上集)失败 → 重试最多 2 次,每次:
- 等 1.5s(让页面状态稳定)
- 重新 `_inject_prompt`(内部 selectAll + delete + insert)
- 等 0.4s,**调 `_get_send_button_state` 预检** — `disabled` 再等 1s,`stop` 再等 3s
- `_dispatch_send` 重发

仍失败 → 调 `_build_degraded_content` 走降级。普通任务保留原"放弃"语义。

**4. `_build_degraded_content` 本地降级**(novel_ai.py L10544+)

只对 `chapter_summary` 做实质降级:章节正文头 300 + 尾 300 + `[降级:v1.91 BUG-065 ...]` 标签,通过 `response_received.emit(task_id, fake_content)` 让 UI handler 走正常 success 路径写入对话记忆 — **保证摘要"绝不为空",事后可手动重生成**。

其他关键任务(canon_extract / character_extract / world_extract / long_term_extract)降级返回空字符串,触发现有 handler 的"AI 无返回"分支,不破坏已有数据。

### 数据流补丁 — `_send_to_ai` 透传 `_xxx` extra(关键陷阱)

写完 _build_degraded_content 时 grep 发现致命问题:`_send_to_ai` 的 `**extra` 只放进了 UI 侧的 `_pending_task_target`,**没传给 worker 的 task dict**。也就是说 worker 调 `task.get("_ch_content")` 永远拿不到 — 降级会退化为纯占位串(没有头/尾正文)。

补丁:`_send_to_ai` submit 时透传 `_xxx` 前缀的 extra 字段:

```python
**{k: v for k, v in extra.items()
   if k.startswith("_") and isinstance(v, (str, int, float, bool, type(None)))},
```

只透 `_xxx` 前缀(约定俗成的"内部 meta")+ 限标量类型(跨线程安全,排除 callable 如 `_done_cb`)。

`_submit_summary_task` 配合改造,把 `_ch_content=ch.get("content", "")` 和 `_ch_title` 显式传进去。

### 编号插曲 — BUG-063 → BUG-065

Claude 第一反应起名 BUG-063,grep 后发现:
- `# ── v1.86 BUG-063:多视角反查 ──`(novel_ai.py L5051)— 已占
- `# ── v1.87 BUG-064:跨表关联可视化(系列收官)──`(novel_ai.py L5312)— 已占
- BUG-065 是空的

字面冲突要避让。这个项目的命名习惯把 feature 也叫 BUG-xxx(BUG-063/064 是多视角反查/跨表关联,不是真的 bug)。**未来起新 BUG 号前必须先 `grep -hoE "BUG-0?[0-9]+" *.py *.md | sort -u`**。

(顺便,v1.89 章节 6277 行记录的"Claude 第一反应是稽核链路漏 BUG-028/029 防御,准备起新 BUG-063"那次 — 当时也是 BUG-063,但因被驳回为"看记忆"而没真正立案。这次是第二次有人差点用错 BUG-063 编号。)

### 测试 + 验证

新文件 `test_bug065_send_retry_degrade.py`(17 测试):
- `_build_degraded_content` 纯逻辑(章节长/短/空正文 / canon/character/world_extract 空降级 / 未知 target 不抛)
- 静态 grep 校验:`_submit_summary_task` 必须传 `_ch_content`、`_send_to_ai` 必须透传 `_xxx`、`_send_prompt` 必须含 `CRITICAL_TARGETS`、`_dispatch_send` 失败 log 必须含"按钮态="

测试架构选择:**用 `ast.get_source_segment` 把 `_build_degraded_content` 源码抠出来 exec 到独立命名空间**,绕开 PyQt5/Selenium 装载(沙箱无 GUI,装不上)。其他依赖浏览器的部分(`_get_send_button_state` / 重试循环)留实战日志验证。

跑:`pytest test_bug065_send_retry_degrade.py test_housekeeper.py` → **36 passed**(17 新 + 19 housekeeper 回归)。

### 给下个 Claude 的提醒

1. **"事后症状判定"是个反模式**。看到"消息计数没变 / textarea 没空"这种"我没看到 X 发生"的判定堆叠时,优先想"有没有直接信号"(当前 DOM 状态、当前按钮态、当前 spinner 在不在)。事后症状全阴性 ≠ 没事发生,可能是你检测的信号本身就不对位。

2. **"放弃"是给非关键任务的语义**。一切跟"对话记忆 / KB / 6 库"相关的后处理任务,失败默认要重试 + 降级,**不能丢数据**。新增类似任务时,先想清楚它该不该进 `CRITICAL_TARGETS`。

3. **跨线程数据传递的隐式断点**。`_send_to_ai` 表面看是统一入口,但它的 `**extra` 进了 `_pending_task_target`(UI 端) **没**进 worker 的 task dict — 这种"看着传了,其实没传"的隐式断点是降级失败的常见根因。**任何新加的 meta 字段都要 grep 一下从 UI → worker 的全路径**,确认每一跳都接住了。

4. **起新 BUG 号前必 grep**。BUG-063/064 都是 feature 名字,不是 bug — 项目命名习惯。新编号取之前 `grep -hoE "BUG-0?[0-9]+" *.py *.md | sort -u | tail` 看最大值 +1。

5. **"批量生成已结束"在单章模式也会出**。这是 v1.91 没修的根因(line 12191 / 17539 两处触发点之一在单章 + chain_to_next 时也会走)。本次靠"重试 + 降级"绕过去了,但根因留给后续 P2 — 单章模式不应该触发批量 cleanup 信号,或者后处理任务应该在 cleanup 信号之前串行跑完。

### 未做(留 P2)

- `_dispatch_send` Enter **之前**没用按钮态预检 — 当前实现只用在重试时。首次发送也可以预检(按钮 `disabled` 就先重新注入再 Enter),但要测会不会跟现有 9020-9048 的"等 AI 空闲"逻辑打架。
- "批量生成已结束"在单章模式的根因(L12191 / L17539)— 治本要重构 `_post_chapter_pipeline` 与 worker 的串行关系,不在本次 scope。
- 摘要重试成功后没回滚已经发出去的"批量已结束"信号 — 用户可能看到"批量已结束 + 摘要重试成功"两条 log 同时出,有点怪。

---

## 一百零八、v1.92 — BUG-066 章节锁定机制(MVP)+ 竞品分析方法论

### 起因

用户上传"由风写作 v1.1.7"功能介绍文档(22877 字符,7-700 字段口语化讲稿风格),让 Claude 筛"可借鉴功能"。这是个典型的**竞品分析任务** — 不是改 bug 不是加单点功能,而是要从一份充满营销话术的产品文档里筛出真正对我们项目有价值的设计思想。

Claude 没有堆砌"可以借鉴!"清单(那是糟糕的回答),而是按"价值 vs 工程量 vs 是否可能已有"做了四档分类:🔥 真高价值 / 💡 中等价值 / 🤔 看现状 / ❌ 不建议。最终筛出来高价值的是「中稿/终稿锁定 + 章节区间审查 + 逐级清除重做」三件套,**关键洞察**是这三个看似独立的功能其实是**同一套工作流的不同切面** — 解决的都是同一个焦虑:"**用户已确认满意的内容,不能被后续审查/修复/重生成误改**"。这跟我们 v1.91 刚补的 BUG-065 "关键任务降级"是**同源思路** — 后者是工程层"做对的事不能丢",前者是产品层"做对的事不能被推翻"。

用户回 "那就做你推荐的。" 7 个字 — Claude 没把所有推荐功能塞进一轮(那是接不住的工程量),而是**分轮规划**:本轮 v1.92 只做锁定 MVP,同名检查 + 角色出退场留 v1.93,区间审查 + 逐级清除留 v1.94。这种"克制做到能用即可,大改分轮"的纪律是这一章的关键。

### 改了什么

**全部改动只触动 `novel_ai.py`(12 处)+ 新增 1 测试文件 + 顺手修 1 旧测试**。

**1. 数据模型**(2 处)
- `add_chapter` (line 12117):新建章节 dict 默认带 `locked: False`
- `_accept_chapter_and_continue` (line 16443):AI 生成入库默认带 `locked: False`

```python
# 之前
chapter = {"title": ch_title, "content": ch_body, "summary": ""}
# 之后
chapter = {"title": ch_title, "content": ch_body, "summary": "", "locked": False}
```

**2. 加载兼容**(2 处:L19497 和 L19791,主路径和 _reset_ui_state 路径)
- 旧存档 chapters list 里的 dict 没有 `locked` 字段 → 加载时 `setdefault("locked", False)`
- 用 `isinstance(_ch, dict)` 防御非字典条目(万一有脏数据不崩)
- **不覆盖已有 `locked=True`** — 这是测试里专门验过的关键不变量

**3. UI 显示**(`_refresh_chapter_list`)
- locked 章节在标题前加 🔒 前缀
- locked 章节 ListWidgetItem 设置 tooltip "已锁定 — save/重命名/删除/写回均跳过。右键解锁"
- **所有判定都用 `ch.get("locked")`,不用 `ch["locked"]`** — 防御 KeyError(测试专门验过)

**4. 右键菜单**(`chapter_list` 创建 + 两个新方法)
- L11653 后加 `setContextMenuPolicy(Qt.CustomContextMenu)` + `customContextMenuRequested.connect(...)`
- 新方法 `_on_chapter_list_context_menu(pos)`:弹出菜单,根据当前 locked 状态展示「🔒 锁定本章」或「🔓 解锁本章」
- 新方法 `_toggle_chapter_lock(idx, locked)`:切换状态 + 刷新列表 + 状态栏提示 + log
- **`QMenu` 顶层未 import**,所以在方法内 `from PyQt5.QtWidgets import QMenu`(沿用 L5189 风格)

**5. 拦截四个改动入口**
| 入口 | 拦截前为 | 拦截后行为 |
|---|---|---|
| `delete_chapter` | 直接 `chapters.pop(idx)` | QMessageBox 提示 + return,不删 |
| `rename_chapter` | 直接 `QInputDialog.getText` | QMessageBox 提示 + return,不改 |
| `save_current_chapter` | 直接 `chapters[i]["title"] = title` | QMessageBox 提示 + return,不写 |
| `_on_chapter_clicked`(切走时写回) | 无脑写回当前章 | locked 章节走 `else` 分支跳过 + log warn |

**6. APP_VERSION**:`v1.91` → `v1.92`

**7. 顺手修旧测试**:`test_bug065_send_retry_degrade.py::TestVersionBumped` 之前是 `assertIn("v1.91", line)` 硬编码 — 每升一版都得改。改成正则提取 `(major, minor)` 然后 `assertGreaterEqual((major, minor), (1, 91))`。**这是测试设计原则:版本下限检查应该用 ≥,不应该用 ==**。

### 关键命名避让

**这是这一章最容易翻车的点**。novel_ai.py 已经有个 `def toggle_lock(self)` 方法(L20002),但它**锁的是编辑器 QTextEdit 的只读状态,不是章节级锁定**。如果新功能也叫 `toggle_lock`,会直接覆盖原方法,导致原功能(L11666 按钮"解锁编辑")挂掉。

避让策略:
- 数据字段:`chapter["locked"]`(类比已有的 `summary` / `hook` 等章节字段)
- 内部方法:`_toggle_chapter_lock`(下划线前缀 + 完整语义)
- 上下文菜单 handler:`_on_chapter_list_context_menu`(明确"章节列表"而非"内容"右键)

测试里专门加了 `test_toggle_chapter_lock_does_not_collide_with_global_toggle_lock` 断言两者并存。

### 不在本轮 scope(留下一轮)

| 留给 | 内容 | 理由 |
|---|---|---|
| v1.93 | housekeeper 感知 locked(oneliner 显示 🔒 / session_summary 区分"已锁定 vs 待审") | housekeeper 是观测层,新增信号需要主流程多挂一个 record_locked() 调用点,需谨慎 |
| v1.93 | 编辑器只读联动(locked 章节切到时自动 setReadOnly(True)) | 涉及 tab_editor 状态机改动,跟现有"解锁编辑"按钮(L11666)的语义要重新理顺 |
| v1.93 | 同名不同姓检查 + 角色出退场字段 | 独立功能,不混入锁定 PR |
| v1.94 | 章节区间审查(指定起始-结束章节) | UI 改动大(需要章节范围选择器) |
| v1.94 | 逐级清除 L1/L2/L3 | 我们的剧情结构分不分层不确定,需调研后再做 |
| v1.94 | AI 审查链 / dialogue_critic autofix 拦截 locked | 等区间审查一起,因为区间审查本身就会读 locked 状态 |

### 测试覆盖(15 条全过)

`test_bug066_chapter_lock.py`,分四个类:

- **`TestChapterLockDataModel`**(6 条):字段默认值、setdefault 行为、JSON round-trip、非字典防御、不覆盖 locked=True
- **`TestChapterLockInterceptionSource`**(4 条):用 ast 抠 delete/rename/save_current/on_clicked 源码,断言 `locked` 检查 + 早 return 必须在破坏性操作(`.pop()` / `QInputDialog` / `title = ...` 赋值)之前 — **源码级别静态验证,不依赖 Qt 运行环境**
- **`TestChapterLockUIDisplay`**(2 条):`_refresh_chapter_list` 必须用 🔒 emoji + `.get("locked")` 防御
- **`TestContextMenuMethodExists`**(3 条):两个新方法存在、不跟原有 `toggle_lock` 撞车、用了 QMenu

**总测试**:本次 15 + BUG-065 17 + housekeeper 19 = 51 条合跑全过。

### 教训 & 下一代 Claude 提醒

1. **竞品分析不是清单堆砌**。用户问"有没有可以借鉴的功能"时,要做的是判断 + 排序 + 联系自己项目当前状态,不是把竞品所有功能复述一遍。Claude 这次按"价值 vs 工程量 vs 同源思路"分类,把高价值的 3 个独立功能识别为同一套工作流,这种**抽象一层的判断**比堆 22 个 bullet 更有用。

2. **大改要分轮,不要塞进一个 PR**。"中稿锁定 + 区间审查 + 逐级清除 + 同名检查 + 角色出退场"5 个功能塞进 v1.92 会失败 — 工程量太大、改动面太广、风险叠加。分轮策略:本轮做数据地基(锁定字段 + UI),下一轮做相关功能(同名检查/角色字段),再下一轮做更复杂的(区间审查/逐级清除)。**每轮必须能独立交付 + 可实战验证**。

3. **新命名前 grep**。这次差点用 `toggle_lock` 覆盖原方法。原方法存在(`grep -n "def toggle_lock" novel_ai.py`)就一秒能查到。**任何疑似"通用动词"的方法名,提交前必 grep**。

4. **测试断言里的硬编码版本号是坑**。`assertIn("v1.91", ...)` 看着没问题,但每升一版都得改一遍 — 既然要改,不如改成 `>= v1.91` 语义,以后免维护。**测试断言应该断"不变量"不是"当前值"**。

5. **数据模型扩字段几乎零成本**(感谢 v1.30 的设计:`"chapters": self.chapters` 直接序列化整个 list)。`add_chapter` / `_accept_chapter_and_continue` 加默认值字面量 + load 兜底 setdefault,**持久化代码完全不用改** — JSON dump/load 自动透传所有字段。这是 monolithic + dict-based 数据模型在小改时的一个隐藏优势,但代价是**字段定义散落各处**(没有 dataclass / schema),所以**下一代 Claude 要加字段时,记得 grep `self.chapters.append`、`{"title":` 全部入库点,不要漏**。

### 未做(留 P2/P3)

- **housekeeper 没集成 locked** — 章末 oneliner 不显示 🔒。下一轮接入:`Housekeeper` 加 `locked` 字段 + `record_locked(bool)` 接口 + render_oneliner 头部 🔒 标记。在 `_accept_chapter_and_continue` 入库后调用 `_hk.record_locked(False)`(新章节肯定不 locked,所以这个调用只是把信号铺出来,真正有用是后续场景)
- **编辑器只读没联动** — 用户切到 locked 章节时,QTextEdit 仍可写(虽然切走时写回会被拦)。下一轮在 `_on_chapter_clicked` 末尾根据 ch.locked 调 `setReadOnly`
- **没有"批量锁定/解锁"** — 竞品支持"全卷锁定"(比如前 20 章一键锁)。我们目前只能单章右键。可以加"工具菜单 → 锁定前 N 章"
- **AI 审查链 / dialogue_critic 没尊重 locked** — `_start_ai_audit_chain` 和 `_on_dialogue_critic_autofix_request` 当前不检查 locked,如果它们会改写章节正文,就是个未来 bug。**实战验证 v1.92 时如果审查链动了 locked 章节,马上加拦截。**

---

## 一百零九、v1.93 — BUG-067 角色 last_ch 字段 + 同名检查(竞品分轮的第二轮)

### 起因

v1.92 推完后用户一句"继续"。这是**竞品分轮规划的第二轮兑现** — v1.92 做锁定,v1.93 按计划做"同名不同姓检查 + 角色出退场字段"两个低成本高收益的小功能。

调研第一步发现一个意外:**`first_ch`(首次出场)字段已经存在** — PROMPTS["world_extract"] 第 282 行模板里就让 AI 填,UI 表格列 7 也显示,序列化 DICT_KEY_MAPS 里也有。也就是说,**"角色出场章节"功能其实早就部分实现了**,v1.93 实际只需要加 `last_ch`(退场章节)— 工程量大幅缩小。这种"以为要从零做、其实只是补半边"的发现是项目里很常见的事,**调研一定要先 grep 现有字段**。

### 改了什么

**全部改动只触动 `novel_ai.py`(9 处)+ 新增 1 测试文件**。

**1. 表格列 8 → 9**(`_build_characters_tab`)
- `QTableWidget(0, 8)` → `QTableWidget(0, 9)`
- HorizontalHeaderLabels 末尾加 `"退场章节"`
- `setColumnWidth(7, 80)` 让 first_ch 从原本"拉伸最后一列"变成固定宽,新加的 last_ch 接管 stretch
- tip label 加一行说明"留空表示尚未退场;AI 不会自动填,需手动标记"

**2. `_add_character` defaults 加 1 元素**(8 → 9)

**3. 序列化路径 8 → 9**(`serialize`):`tbl_to_list(self.tbl_chars, 8)` → `9`

**4. `DICT_KEY_MAPS["characters"]` 加 `"last_ch"`** (放 `first_ch` 之后)

**5. `DICT_KEY_MAPS_LOCAL["characters"]` 同步加**(load 用的局部映射,这里不同步会导致 normalize 转出 8 元素 list 而 list_to_tbl 期望 9 → last_ch 永远不被填)

**6. `load` 路径 vals 加 last_ch**:`str(c.get("last_ch", ""))` 兜底空字符串

**7. `_on_check_duplicate_names` UI handler**:读 tbl_chars → 调纯函数 → QMessageBox 弹结果(无冲突信息提示 / 有冲突 warn 列出每组)

**8. `_find_duplicate_names(rows_data)` @staticmethod**:纯函数,无 Qt 依赖,便于单测。返回 `{name: [(idx, role), ...]}` 只含 ≥2 次出现的 name。空名不算冲突(还没填好的行)。`name.strip()` 防御前后空格漏检。

**9. 顶部按钮**:`"🔍 同名检查"`(`_build_characters_tab` 顶部 button area,跟 ➕ 新增 / ➖ 删除并列)

### 关键漏改修复 — 两个隐藏的 8 列硬编码

**这是这一章最大的工程教训**。我以为改完 6 个明显点就结束,grep 后发现还有两个:

**(a) L6028 `_tbl_to_rows(self.tbl_chars, 8)` — 故意保持 8** ✅

这个调用在 `_refresh_relation_graph` 里,把 chars_rows + relations_rows 喂给 `relation_graph.build_graph_data`。该函数文档注释明确说 "chars_rows 每行 [姓名, 角色定位, ..., 首次出场] 8 列",且实现里有 `row[i] if i < len(row) else ""  for i in range(8)` 截前 8 列。

**结论**:保持 8 是对的,relation_graph 不需要 last_ch。但要**加 v1.93 注释明示故意保持**,否则下一代 Claude 看到 "tbl_chars 是 9 列但这里传 8" 会困惑甚至误改。

**(b) L6158 `list_to_tbl(self.tbl_chars, ..., 8)` — 必须改 9** ⚠️

这是 `load` 方法里的填表调用。配合 `DICT_KEY_MAPS_LOCAL["characters"]` 现在是 9 字段,`normalize` 会转出 9 元素 list,如果 `list_to_tbl(tbl, rows, ncol=8)` 只填前 8 列 → **last_ch 列永远是空**,**这是个沉默的 bug**(不报错,只是数据丢)。

测试加专门一条 `test_load_path_uses_9_columns` 静态断言这里必须是 9,防回归。

### 另外两个"故意 8"的 prompt 注入路径(L6755 / L15791)

这两处是构造给 AI 的角色档案 prompt:

```python
row = [self.tbl_chars.item(r, c).text() if self.tbl_chars.item(r, c) else ""
       for c in range(8)]
name, role, look, pers, mark, ability, state, _ = row
```

`unpack` 8 个变量,末尾 `_` 是 first_ch(本来就不用)。**保持 8 是对的** — prompt 现在不需要告诉 AI "某角色已退场",但**这是未来扩展点**:如果想让 AI 看到 last_ch 来避免让退场角色乱入,把 range(8) 改成 range(9),unpack 加一个 `last_ch` 变量,然后在 prompt 文本里加"已退场角色不要让他/她在本章出现"提示即可。

**v1.93 不动这两处** — 等 v1.94 的"角色出退场字段被 AI 使用"功能时一起做。

### 测试 24 条全过

`test_bug067_charlib_lint.py`,五个类:

- **`TestLastChDataModel`**(5 条):DICT_KEY_MAPS 主/局部都有 last_ch + 加载兜底 + JSON round-trip + vals 顺序(first_ch 列 7,last_ch 列 8)
- **`TestFindDuplicateNames`**(9 条):无重名 / 单对重名 / 三同名分组 / 混合 / **空名不算** / **空格 strip 后才算** / 空表 / 单角色 / 大小写敏感
- **`TestCharLibrarySourceUI`**(6 条):列数 9 / header 顺序(首次出场 紧跟 退场章节)/ defaults 长度 9 / 按钮存在 / serialize 用 9 / **load 用 9(防漏改回归)** / relation_graph 路径故意保持 8 / tip 提到 last_ch
- **`TestNoCollisionWithV192`**(2 条):章节锁定的 `_toggle_chapter_lock` / `_on_chapter_list_context_menu` 方法仍在 + 章节默认 locked=False 没动

**合跑 75 条全过**:24 BUG-067 + 15 BUG-066 + 17 BUG-065 + 19 housekeeper。

### 教训 & 下一代 Claude 提醒

1. **改字段必 grep 列数硬编码**。v1.93 的关键漏改不是"想不到",而是**只改 serialize/load 主路径就以为完了**。`_refresh_relation_graph` 和 `_build_*_prompt` 这些**消费方**也读 tbl_chars,只是用不同的 ncol。任何加列的改动,必须 grep `tbl_chars.*[0-9]` / `range\([0-9]+\)` 找所有列号 / `tbl_chars.item(r,\s*[0-9]+)` 找列索引访问,逐个判断"故意 N"还是"漏改"。

2. **"故意 N"要加 v1.93 注释**。这次我在 L6028 加了 "v1.93 BUG-067:故意保持 8 — relation_graph.build_graph_data 接口按 8 列设计" 的注释 — 不然下一代 Claude 看到 "tbl_chars 9 列但这里 8" 会困惑或误改。**对一致性的偏离,必须留 paper trail。**

3. **沉默 bug 比报错 bug 更危险**。L6158 漏改不会让代码崩,只是 last_ch 永远填不进表 — 用户看到表里这列总是空,以为"AI 没填、自己也填不进去",其实是写了被丢。这种"看起来在工作"的 bug 用静态测试断言专门防(`test_load_path_uses_9_columns`)。

4. **staticmethod + 测试 setUpClass 的 staticmethod 包装**。把同名检查核心逻辑 `_find_duplicate_names` 提成 @staticmethod 是为了纯函数单测,但抠源码 exec 后赋给 cls 会被 Python 解释为 bound method,调用时自动塞 self → TypeError "takes 1 positional argument but 2 were given"。**修法:`cls.find_dups = staticmethod(ns["_find_duplicate_names"])`**,显式包 staticmethod 才避免 bound。这是个对 Python 描述符协议的小坑,值得记。

5. **测试断言里别 grep 太短的字符串**(`test_table_header_includes_last_ch_label` 的首次教训)。`chunk.find('"退场章节"')` 会匹配到注释里的 `"退场章节"`(注释字符串里也用双引号),不是 header labels。**改用更具体的连续字符串**:`'"首次出场", "退场章节"'`(两个字符串相邻,只在 header labels 才会出现)。

6. **每轮改动严守"不动上轮区域"纪律**。v1.93 不动章节相关代码、v1.92 不动 worker 相关代码、v1.91 不动 housekeeper。这样**任何一轮如果实战发现问题可以独立回滚**,不会拖累其他轮。测试 `TestNoCollisionWithV192` 是这条纪律的物化 — 静态断言上轮关键方法 / 字段仍在。

### 未做(留下一轮)

- **last_ch 字段没让 AI 用**。L6755 / L15791 的 prompt 注入仍 unpack 8 列,AI 看不到哪些角色已退场。下一轮把这两处的 range(8) 改 9,unpack 加 last_ch 变量,prompt 文本加"已标记退场章节的角色,如果当前章节号 > 退场章节号,不要让其出现"提示。
- **同名检查没集成进 housekeeper**。当前只是 UI 按钮手动跑。可以接入 `_accept_chapter_and_continue` 末尾自动跑一次 + record_warn 给章末 oneliner — 但要小心不要每章都弹窗,可能改用"静默 warn,只在 oneliner 加 🔍N 标记"。
- **包含/被包含的近似重名没检测**。"林清" vs "林清歌" 这种 AI 可能也会混淆但 v1.93 MVP 没覆盖。v1.94 可以加 fuzzy 匹配(Levenshtein 距离 ≤ 2 或一方是另一方前缀)。
- **角色按出退场范围过滤**。**这个是 last_ch 字段最大的用途** — 写第 50 章时,自动给 AI 注入"当前在场角色 = 所有 first_ch ≤ 50 且 (last_ch 为空 或 last_ch ≥ 50) 的角色"。这个改完后,AI 写正文不会让退场角色乱入,前面提到的 prompt 改造可以一并做。

---

## 一百一十、v1.94 — BUG-068 下一章选项按钮对比度修复(1 行 CSS,但值得记)

### 起因

用户上传截图说 "下面按钮看不清"。截图是章节末"📌 本章元信息(钩子/爽点/伏笔/下一章选项)"面板,三个"下一章选项"按钮文字几乎不可见 — 浅米色按钮 + 浅灰文字。

这种 UI bug 修法只有一种(加 color),但**值得当成一个独立 BUG 记录**,原因有二:(a) 它揭示了一条**Qt setStyleSheet 的陷阱**(漏写字段 → 用默认值 → 可能跟你想象的不一样),下一代 Claude 会遇到;(b) 截图反馈这种"看得见但形状不对"的 bug 很难通过测试或代码 review 发现,**必须有视觉反馈才能暴露** — 这给以后"实战验证"环节加了个提醒:**截图比 log 重要,任何 UI 改动后用户跑实战时,Claude 应该主动问"截图给我看一眼"**。

### 改了什么

**全部改动 2 处 + 1 测试**:

**1. APP_VERSION** v1.93 → v1.94

**2. `ChapterEditor._set_pangu_meta_display` 行 2062-2065 按钮样式**

改前:
```python
btn.setStyleSheet(
    "QPushButton { text-align:left; padding:4px 8px; "
    "background:#fff8ea; border:1px solid #e0c896; }"
    "QPushButton:hover { background:#ffe9b8; }")
```

改后:
```python
btn.setStyleSheet(
    "QPushButton { text-align:left; padding:4px 8px; "
    "color:#3a2a10; "                                   # ← 新增:深棕,WCAG AAA
    "background:#fff8ea; border:1px solid #e0c896; }"
    "QPushButton:hover { background:#ffe9b8; color:#000; }")  # ← hover 也加 color
```

颜色选型:
- 正常态 `#3a2a10`(深棕)配 `#fff8ea`(浅米)对比度计算约 13:1,远超 WCAG AAA(7:1)
- hover 态 `#000`(纯黑)配 `#ffe9b8`(更深米)对比度更高,给明确点击反馈

### 关键证据 — 这不是风格选择,是真 bug

Panel 内其他 label(钩子/爽点/伏笔/tip)**全部显式 color**:
- L1860 `self.pangu_hook_label.setStyleSheet("color:#444; padding:2px 4px;")`
- L1864 `self.pangu_cool_label.setStyleSheet("color:#444; padding:2px 4px;")`
- L1867 `self.pangu_seeds_label.setStyleSheet("color:#444; padding:2px 4px;")`
- L1871 `nl.setStyleSheet("color:#666; ...")`
- L1855 tip `setStyleSheet("color:#1a4480; ...")`

说明 panel 的设计语义本来就要求**显式 color**(因为 Qt 默认前景色受全局主题影响,不可控)。**按钮漏写就是漏写,不是有意为之**。测试里专门加一条 `test_panel_labels_already_have_color` 验证这个"对照证据"。

### 教训 & 下一代 Claude 提醒

1. **setStyleSheet 漏字段 = 用默认值,默认值不一定是你想的**。改 Qt 样式时,如果你写了 background 没写 color,Qt 会用全局主题的默认前景色 — 在不同主题/不同 Qt 版本下可能是黑、灰、白。**只要面板有自定义 background,就应该同时显式 color**,否则就赌默认前景色跟你的背景配得上。

2. **批量样式定义看"邻居"的一致性**。这次 panel 内其他 5 个 label 都显式 color,只有 1 个按钮没写 — 这种"邻居不一致"的 setStyleSheet 是个 hint:**漏写**。改样式时如果发现写法跟邻居不同,先问一句"为什么不同",再决定是模仿邻居还是有意特立独行。

3. **UI bug 难被测试 & code review 抓到,必须截图**。这次 bug 的 setStyleSheet 字符串本身完全合法,py_compile 不会挂,unittest 跑不出问题,逻辑测试覆盖不到 CSS 视觉效果。**只有用户实战截图反馈才暴露**。这给"实战验证"环节加个提醒:**Claude 在请用户跑实战时,如果改了 UI,主动让用户截图,不要光要 log。**

4. **小 bug 也升一个版本号 + 写记忆**。v1.94 只动了 2 行代码,但仍升版本号、立 BUG-068、记一章。原因:(a) 项目惯例每个 BUG 升一版,保持节奏;(b) 这种"漏写"陷阱值得留记忆,不然下一代 Claude 改其他 setStyleSheet 时还会重蹈覆辙。**记忆的价值不在文字长度,而在"踩过的坑明确标注"**。

### 未做(留下一轮)

- **没全局扫 setStyleSheet 漏 color**。novel_ai.py 里 setStyleSheet 调用数十处,其他地方有没有类似漏写不可知。下一轮如果有空,可以做个 grep — 找 `setStyleSheet` 调用,看哪些有 `background:` 但没 `color:` 的。但 priority 低,因为大多数地方默认前景色应该够用,这次是 panel 用了浅米色背景比较罕见。
- **没改主题切换适配**。如果用户用深色主题,这个 panel 仍然是固定米色 — 设计意图就是"突出元信息",所以暂不动。如果以后要做"米色面板深色主题变种",再加 QPalette / 主题切换信号。

---

## 一百一十一、v1.95 — BUG-069 字数判定三档(A→B→C→D 路线第一步)

### 起因 — 实战日志暴露的产品决策问题

v1.94 推送后用户发了实战日志。日志里第 4 章生成完整字数 3088 字,但目标 2300 字,housekeeper 判 `字数✗3088`(超出 34%)。**章节质量没问题**(死磕 3 次最终 Canon 校验通过、钩子爽点伏笔都入库),只是字数偏多。

但 oneliner 显示 `✗` 给用户一个错觉:"东西坏了"。这是**产品决策问题**而不是工程 bug — 字数门当前规则只看下限 `actual >= 0.8 * target`,**没有上限概念**,所以"达标了但写多了"跟"真的不达标"用同一个 ✗。

用户的指令很简单:"A → B → C → D 一步一步来" — 我之前列了 4 个选项,A 就是这个字数判定改动。**用户没拍板具体怎么改,所以 Claude 自己拍板了中庸路线**(在文档里有这种"用户没指示就走中庸"的清晰记录,免得下一代 Claude 困惑)。

### 改了什么

**全部改动只在 `housekeeper.py`(3 处)+ `novel_ai.py`(1 处版本号)+ 1 测试**。

**1. 数据类加新字段**(L72-74):
```python
word_count_long: bool = False  # 超长但质量没问题(actual > 1.5x target)
```

**关键设计:加新字段而不是改 word_count_ok 的语义**。这样既有 19 条 test_housekeeper 测试**一字未改全过**,完美兼容。`word_count_ok` 仍然只表示"下限达标",新增 `word_count_long` 标记超长,oneliner 用两个字段组合显示三档。

**2. `record_word_count` 加上限判定**(L257-282):

```python
if target > 0:
    lower = int(target * 0.8)
    upper = int(target * 1.5)
    self.current_report.word_count_ok = (actual >= lower)
    # 超长只在 ok=True 基础上叠加 — 不够的章节,主要矛盾是不够,不再额外标 long
    self.current_report.word_count_long = (
        self.current_report.word_count_ok and actual > upper)
else:
    self.current_report.word_count_ok = None
    self.current_report.word_count_long = False
```

**`long` 字段必须以 `ok=True` 为前提** — 因为"既不够又超长"是不可能的状态,但代码层加这个 guard 让组合状态清晰。

**3. `render_oneliner` 三档 emoji**(L110-119):

```python
if self.word_count_actual > 0:
    if not self.word_count_ok:
        wc_mark = "✗"        # 不够(< 0.8x)
    elif self.word_count_long:
        wc_mark = "⚠"        # 超长但达标(> 1.5x)
    else:
        wc_mark = "✓"        # 合理(0.8x - 1.5x)
    bits.append(f"字数{wc_mark}{self.word_count_actual}")
```

**4. `_compute_health` 不变** — 因为 `word_count_ok` 语义没改,只看它扣分,超长 `long=True` 不影响。**实战 3088/2300 现在从 ✗ 扣 0.3*0.4=0.12 健康度 → ✓ 不扣分**,健康度从 0.73 直接回到 0.85+(绿区)。

### 关键决策点 — 为什么阈值是 0.8x / 1.5x?

- **下限 0.8x**:**保留原行为**(原代码就是 0.8),不动用户已经习惯的判定标准。
- **上限 1.5x**:**新增**。1.5 是个常用的"宽容上限"系数 — 用户写 2300 字目标,2300×1.5=3450 字封顶。如果 AI 写出 4000+ 字,说明任务理解可能偏了(prompt 没控制好长度),值得 ⚠ 提醒;但 3000-3450 之间在正常波动范围,不该报警。
- **边界包含/排除**:上限用 `>`(>1.5x 才算超长,= 1.5x 不算)、下限用 `>=`(0.8x 算达标)。测试里专门验过两个边界 `test_exact_lower_boundary` + `test_exact_upper_boundary` + `test_just_above_upper_boundary`。

### 测试 19 条全过

`test_bug069_word_count_overlong.py`:

- **`TestThreeTierWordCount`**(9 条):三档分别正确 + 实战数字 3088/2300 → ✓ + 边界 0.8x = / 1.5x = / 1.5x+1 → / target=0 → / 字段默认值
- **`TestOnelinerEmojis`**(4 条):正常 ✓ / 超长 ⚠ / 不够 ✗ / 实战数字应该出 ✓ 不是 ✗
- **`TestHealthScoreUnaffectedByOverlong`**(2 条):合理 vs 超长同分(都 1.0)+ 不够仍扣
- **`TestNoCollisionWithPreviousMilestones`**(4 条):APP_VERSION ≥ v1.95 + v1.94 按钮色仍在 + v1.93 last_ch / 同名检查仍在 + v1.92 章节锁定仍在

**合跑 101 条全过**(19 BUG-069 + 7 BUG-068 + 24 BUG-067 + 15 BUG-066 + 17 BUG-065 + 19 housekeeper)。

**最值得标注**:既有 19 条 housekeeper 测试**一字未改全过** — 这是"加字段而非改语义"设计的回报,**下一代 Claude 加字段时记住这条:扩展靠新增,不靠修改既有字段的含义**。

### A→B→C→D 路线进度

| 步 | 内容 | 状态 |
|---|---|---|
| **A** | 字数判定改产品决策(本轮 v1.95)| ✅ 完成 |
| B | 单章模式"批量已结束"治本(L12191 / L17539 根因)| ⏳ 下一轮 |
| C | 区间审查 + 逐级清除剧情树(竞品三件套剩余两件)| ⏳ |
| D | 让 AI 用 last_ch(L6755 / L15791 prompt 8→9 + 写"已退场角色不要乱入")| ⏳ |

### 教训 & 下一代 Claude 提醒

1. **产品决策没明示就走中庸**。用户说"A→B→C→D 一步一步来",但 A 本身有三个子选项(加上限/不改/改 message),用户没明示。Claude 选了"加上限 + 三档 emoji 区分"的中间路线。**这种自主决策必须在记忆里留 paper trail**:"基于什么信息选这个、不选别的",免得用户回头问"为啥这么做"或下一代 Claude 看不懂。

2. **扩展靠新增字段,不改既有语义**。`word_count_ok` 改成 enum 三值 / 改成 int / 改成 "ok"/"warn"/"fail" 字符串 — 都会破坏既有测试和代码。**加新字段 `word_count_long: bool` 是最稳的扩展**:旧代码看 `word_count_ok` 仍然得到 True/False/None,新代码看两个字段组合得到三档。这条原则也适用于章节 dict / 角色 dict 等所有 schema-less 数据结构。

3. **边界用 `>=` 还是 `>` 都要测**。`0.8x` 用 `>=`(下限包含)、`1.5x` 用 `>`(上限不含)。两边规则不对称是有意的:下限是"达标线"(到了就算过)、上限是"警戒线"(过了才报警)。测试覆盖两个边界 + 边界+1,避免后来人改成对称结构后没人发现。

4. **健康度扣分逻辑改字段时一定要 grep 调用方**。我这次没改 `word_count_ok` 含义,所以 `_compute_health` 里 `if self.word_count_ok is False: hard_ratio -= 0.3` 这条不需要动 — 但如果我把 `word_count_ok` 改成 enum,这里就会挂。**改任何核心字段前,grep 一次它在文件里的所有用处**。这次没踩坑是因为采用了"加新字段"的策略,但下一代 Claude 改其他字段时记得 grep。

### 未做(留下一轮)

- **`word_count_long` 没接入主程序日志**。novel_ai.py 里有几处直接读 `word_count_ok` 的代码(grep 一下确认),这次没改它们 — 因为 housekeeper 的 oneliner 已经在 `record_word_count` 自动算出 ⚠,主程序 log "✓ 第 4 章生成成功!字数:3088 字" 那条仍是原文案。如果用户希望"主程序日志也显示超长警告",下一轮做。
- **没加用户可配阈值**。0.8x / 1.5x 是硬编码,某些用户可能要求"更严"(1.2x 就算超长)或"更松"(2x 才超长)。可以加 QSettings 配置,但这次不做 — 默认值就够多数用户用。
- **章节内的"超长"和"伏笔状态"等其他维度没关联**。如果章节超长是因为伏笔堆得太多(meta 里有 5+ 个 seeds_planted),其实可以联合分析。这种"健康度多维评分"留 P3。

---



---

## 📚 归档索引

老 BUG 修复历史(BUG-001~064 / v1.74~v1.89 / 共约 6050 行)已归档到:

- `docs/bugs/MEMORY_ARCHIVE_pre_v2.0.md` — 完整的 v1.x 时代开发记录

  包括:盘古集成自查 BUG / 外部审计 10 BUG / 运行时实测 / 第五~六批改动 / 4K HiDPI /
  小说封面 Tab 删除 / DeepSeek 适配 / 钩子/爽点 / 137 禁用词 / 13 法对话 / 30 项质检 /
  TTS / 黑夜模式 / 存档结构改造 / 拆书学习 / 项目主页 / 关系网 / 战力体系 /
  "做没有的" 4 步(伏笔/承诺/进度/信息隔离/剧情树)/ POV 模式 / 写作回流 /
  跨表关联可视化 / workflow_pipeline / BUG-028 回归

  按需翻阅。日常开发不需要读完。

历史 docs 在 `docs/archive/`(5 份 v7 时代接入指南),详见 `docs/archive/README.md`。



**最近更新**:2026-05-26 — **v2.23.0 🚀 新功能 + bug 修复**。两组改动一次发:① **番茄榜单扫描增强灵感生成** — 用户点"生成灵感"时,如果平台是番茄+浏览器就绪,worker 自动 navigate https://fanqienovel.com/rank?enter_from=menu 抓 Top50 榜单,按用户题材过滤(hard match 不够 10 条放宽相近题材),提取 Top10 题材组合 + Top15 卖点关键词,拼增强 prompt 喂 AI(不传具体书名,防学样)。30 分钟缓存,失败兜底到旧通用 prompt。新增 `core/fanqie_rank_scraper.py` 纯逻辑模块(460+ 行,不依赖 Qt 可独立单测)+ worker `scrape_fanqie_rank` action + `rank_scraped = pyqtSignal(str, list)` 信号。② **钩子检测松绑(BUG-086 第二组)** — 修第 1 章死磕 10 次问题:章里"倒计时 ★★★★★"钩子已入库但 HOOK_MARKERS 关键词未覆盖,导致写后校验持续失败,浪费 ~7 分钟 + 大量 token。加 `_extract_hook_intensity_from_text` helper 解析 `【断章钩子】` 元信息块,强度 ★★★★+ 时直接放行(日志说明)。228 测试零退化(197 既有 + 31 新)。**教训**:① 纯逻辑模块跟 Qt 解耦能让单测大幅简化;② 第三方网站爬虫 selector 必带多重候选 + 启发式兜底;③ 跨进程信号载荷只用基本类型(传 dict 不传自定义类);④ 元信息 vs 关键词扫描的双判定不一致是常见坑,要明确谁说了算。`前次:2026-05-26 — v2.22.3 BUG-084/085 双修`。

如果你(未来的 Claude)做了重大改动,请:
1. 更新顶部 **最新里程碑**
2. 在 **文件结构** 加新文件,在 ⭐ 标新增模块
3. 在 **PROMPTS 字典** / **GUI 接入** 表加新条目
4. 在 **关键设计决策** 加新约束(如果引入了新的"不能打破"的约定)
5. 在 **还可以做的方向** 把已完成项加 `~~` 删除线,标 ✅
6. **推送这个文件到 GitHub**(让记忆持续)

---

*最后更新:2026-05-26 / **v2.23.0 新功能 + bug 修复**:① 番茄榜单扫描增强灵感生成(新增 core/fanqie_rank_scraper.py 纯逻辑模块 + worker scrape_fanqie_rank action + 30 分钟缓存 + 失败兜底)② 钩子检测松绑(元信息 ★★★★+ 直接放行,修第 1 章死磕 10 次)。228 测试零退化(31 BUG-086 守护新加)。*
*由 Claude 与 qiaodaxian233 协作生成*
