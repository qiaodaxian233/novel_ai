# v1.98 BUG-072 对话记忆

> 目的:任何一次会话 token 耗尽 / 沙箱重置后,下一轮靠这一个文件就能接上。

## 本轮起点

- 远端 HEAD:**v1.97** (`143de25`)
- 上一轮做过 v1.98 但**没 push**,沙箱重置全丢
- 上一轮在 v1.98 改动后跑 BUG-071 出现 1 个失败,**无法判断**是改动破坏还是测试自己写错(没推上来)
- 上一轮已验证 v1.97 自身的 BUG-071 baseline 21/21 全过 — 远端干净

## 目标:v1.98 BUG-072

新增"AI 文风套话"禁用词类别。

- 禁用词总数 117 → 123(+6)
- 6 个新词:动手时从 `pangu_system.py` 现状决定
- 同步改 `pangu_system.py` docstring(类别清单 + 总数) **和** `PanguEngine._FORBIDDEN_WORDS` list
- 句式铁律段后插【AI 文风防线】小段。**不带 L 编号**(对话 13 法已占 L1-L13,避免冲突)
- `APP_VERSION` → `v1.98`

## 上一轮已定决策(本轮沿用,不重议)

- P1 = **a** 保留盘古直给
- P2 = **a** 改动进 `pangu_system.py`,**不**进 `novel_ai.py`
- P3 = **a** 全补 6 个禁用词

## 11 步执行计划

| # | 步骤 | 状态 |
|---|---|---|
| 0 | 写对话记忆 | ✅ 进行中 |
| 1 | ~~验旧 PAT 死透~~ + clone(新 PAT 已用) | ✅ |
| 2 | BUG-071 baseline 21/21 pass | ✅ |
| 3 | docstring 117→123 + 新类别 | ✅ |
| 4 | 插【AI 文风防线】段(无 L 编号) | ✅ |
| 5 | `_FORBIDDEN_WORDS` list +6 词 | ✅ |
| 6 | `APP_VERSION` → v1.98 + 同步 BUG-071 测试 | ✅ |

**已知 TODO**:`test_bug071_pending_task_targets_dict.py` 用 `assertIn('APP_VERSION = "vX.Y"', SOURCE)` 硬钉版本号,每次升版都得手动同步。应重构成其他 bug 测试用的"解析后 ≥"模式。不在 v1.98 范围。
| 7 | 写 BUG-072 测试(24 个) | ✅ |
| 8 | BUG-072 测试 24/24 pass | ✅ |
| 9 | 全套回归(BUG-065~072 + pangu_system + pangu_patch) | ⬜ |
| 10 | commit + push + fresh clone 远端二次确认 | ⬜ |
| 11 | 清场 + 用户撤新 PAT | ⬜ |

## 纪律铁律(都是踩过坑的)

- **PAT 不复用,用完即撤** — push 完 + 二次确认 + 清场后用户撤 PAT
- **PAT 不写进任何文件、不进仓库** — 包括这份记忆。会话内可见即可
- **沙箱会话之间会重置** — 没 push 等于没做
- **fresh clone 二次确认** — 不能只信本地 push 报告,重新 clone 一份验
- **看真实结构再动手** — 上一轮栽过:`install_pangu` 在 `pangu_patch.py` 不在 `pangu_system.py`
- **section 切分** — 别用 `find("【", start)` 找下一段,段内引用会切短;用 `\n【` 作段首边界
- **测试失败先怀疑测试自己** — 上一轮 BUG-072 第一次 22/23 是测试 section 切分错,不是被测代码错

## 关键定位

- 禁用词清单(docstring):`pangu_system.py` 顶部 module docstring,有"117 个"字样
- 禁用词列表(Python):`pangu_system.py` 的 `PanguEngine._FORBIDDEN_WORDS`(list)
- 铁律段插入点:"句式铁律"段后,"对话铁律"段前
- 情绪铁律:`pangu_system.py` L81-L83,**不要动**
- install_pangu wrap 机制:`pangu_patch.py`,`head = PANGU_CORE_RULES + "\n"`,然后 chapter / golden_three 都拼 `head + original + tail_*`
- BUG-072 测试模板:参考 `test_bug071` = unittest + 静态扫 6 词 + install_pangu 端到端 + 路线纪律 v1.92-v1.97 完整性

## token 节省纪律(本轮强制)

- 每步**1-2 个工具调用**,做完停,等用户说"继续"再下一步
- 报告**1-2 句话**说结果,不贴文件、不列长表
- 不写中间"自检小结"
- 出问题才展开
- emoji / 装饰符号一概不用

## 额度告急应对

token 即将耗尽时:
1. **立即停手**,不开新工具调用
2. **更新本文件"当前进度"段**,标清停在哪步、哪个文件改到一半
3. PAT 已在 `.git/config`:`commit -am "wip: v1.98 BUG-072 [进度]" && push`,记忆和半成品一起推上去
4. PAT 未进 `.git/config`:`present_files` 给用户,下一轮带回来

## 当前进度

- v1.98 BUG-072 未开始
- 本文件刚写完,等"继续"开始第 1 步
